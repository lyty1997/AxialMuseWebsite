# 待决策问题

状态：active  
最近更新：2026-07-28

## 已由用户确认

- **D-001 / 2026-07-12**：正式域名为 `axialmuse.com`。
- **D-002 / 2026-07-12**：域名已在腾讯云注册。
- **D-003 / 2026-07-12**：用户声明该域名已完成中国大陆 ICP 备案。
- **D-004 / 2026-07-12**：已购买腾讯云轻量应用服务器，首版改用中国大陆自托管路线。
- **D-010 / 2026-07-13**：轻量实例地域为中国上海，操作系统为 Ubuntu Server 24.04 LTS 64bit。
- **D-011 / 2026-07-13**：服务器当前为空机，专用于本网站部署。
- **D-012 / 2026-07-13**：ICP 备案号为 `沪ICP备2026029086号`，用户确认腾讯云接入备案成功。
- **D-013 / 2026-07-13**：关于区展示 GitHub 个人主页 `https://github.com/lyty1997`，仅作为公开访问链接。
- **D-014 / 2026-07-13**：为各项目提供 `https://<project-slug>.axialmuse.com/` 独立体验入口。
- **D-017 / 2026-07-13**：首个项目为 DocRestore，保留名称 `docrestore.axialmuse.com`，公开仓库 `https://github.com/lyty1997/DocRestore`，生产分支 `main`，构建目录 `frontend`，静态产物 `frontend/dist`。
- **D-018 / 2026-07-13**：DocRestore 的 GPU OCR、API、WebSocket、上传、任务和数据存储可在自有服务器私有运行，仅用于开发和录制演示，当前不作为公共服务入口。
- **D-020 / 2026-07-13**：DocRestore 首版不提供在线体验，不创建 `docrestore` 或 API 子域名的 DNS、Nginx、证书和部署；主站首次上线展示项目说明和 GitHub 仓库，演示视频作为素材完成后的增量增强。
- **D-021 / 2026-07-13**：演示视频默认使用主站原生 `<video>` 控件，不自动播放、不嵌入第三方 iframe，并要求封面、中文字幕、文字摘要和逐帧隐私审核。
- **D-022 / 2026-07-13**：主站第二个项目为 VibeCoding Project Scaffold，仓库 `https://github.com/lyty1997/project-scaffold`，生产分支 `main`；首版只展示项目说明和“查看源码”。
- **D-023 / 2026-07-13**：主站项目与子域名体验拆分为两个注册表；VibeCoding Project Scaffold 不创建子域名、不提供在线体验，也不要求演示视频。
- **D-027 / 2026-07-14**：采用“静态主站 + 中央身份服务 + 独立评论服务 + 各项目独立服务”的目标架构。主站与动态服务分离，一个中央账户未来可访问全部项目；当前阶段只建设静态主站，不部署登录、评论或项目试用后端。
- **D-028 / 2026-07-14**：技术分享采用 Git 管理内容并由静态站点生成器构建；不采用手工 HTML 作为长期内容发布方式，也不在当前阶段引入私有 CMS。
- **D-029 / 2026-07-14（已被 D-051 替代）**：静态站点生成器选择 Astro 静态输出模式。构建结果交给 Nginx 提供，生产请求链路不运行 Astro 或 Node.js 服务；未来登录和评论通过独立服务接入，不把主站改成统一运行时后端。D-051 后续只替换目标框架，静态生产边界继续有效。
- **D-030 / 2026-07-14**：普通技术文章默认使用 Markdown；只有确实需要文章专属组件时才使用 MDX，并且 MDX 只能引用仓库内经过审核的组件。组件目录、审核登记机制、UI 框架和客户端加载策略仍需另行确认。
- **D-031 / 2026-07-15（路径职责保留，尾斜杠语义已由 E-002 补充）**：主站使用 `/`、`/projects/`、`/projects/<project-slug>/`、`/writing/`、`/writing/<article-slug>/` 承担首页、项目目录、项目介绍、技术分享目录和文章正文；未来项目真正提供试用后才启用 `https://<project-slug>.axialmuse.com/`。当前不为项目试用创建 DNS、证书或入口。本决定形成时没有单独比较末尾 `/` 是否属于 canonical URL 契约，D-058 也不从表格记法推导站点级策略；E-002 已在 2026-07-18 明确 `trailingSlash: true`，因此该项不再等待用户决策。
- **D-032 / 2026-07-15**：文章正文页和项目介绍页采用经典三栏信息结构：左栏为目录，中栏为正文，右栏为辅助区域。左右栏职责由 D-033 确认，响应式行为由 D-034 确认。
- **D-033 / 2026-07-15**：三栏采用文档站式职责。顶部导航负责全站跳转；文章页左栏显示技术分享目录，项目页左栏显示项目目录并高亮当前项；中栏显示正文；右栏主要显示当前页面 `H2/H3` 标题导航，可在下方显示少量上下文元数据。短页面不制造无意义目录，可只显示元数据或留空。
- **D-034 / 2026-07-15**：三栏采用渐进式折叠。宽屏显示完整三栏；中等宽度折叠左栏为正文上方默认收起的“浏览本栏目”，保留中栏与右侧标题导航；窄屏改为单栏，“浏览本栏目”和“本页目录”均在正文上方默认收起。优先使用原生可访问折叠控件，不使用遮挡正文的抽屉；精确断点通过视觉稿和实际截图确定。首版标题导航仅使用静态锚点，不做依赖客户端 JavaScript 的滚动高亮。
- **D-035 / 2026-07-15（文章字段值形态已被 D-058 部分替代）**：项目与文章使用手工英文语义 slug，只允许小写 ASCII 字母、数字和连字符；URL 不包含日期、分类或系列路径，也不从标题自动生成。发布前由作者确认，发布后保持稳定；确需修改时必须为旧 URL 配置永久重定向并同步站点地图。项目逐项登记稳定英文标识，例如 `docrestore`。D-058 后续把文章 frontmatter 的整个 `slug` 值改为带固定 `/writing/` 前缀的原生根相对路径；本条的短标识字符规则继续约束 `<article-slug>` 路径段和项目 slug，不再描述文章 `slug` 字段的完整字符集合。
- **D-036 / 2026-07-15**：技术文章采用完整编辑模型，元数据能力覆盖核心发布信息、作者、分类与主题、项目与系列关系、SEO 覆盖、推荐控制和公开修订记录。该决定只确认模型完整度与能力边界；字段名称、必填性、基数、嵌套方式、枚举和校验规则仍需逐项确认。
- **D-037 / 2026-07-15**：技术文章元数据采用分组式 frontmatter，与正文保存在同一个 Markdown 或 MDX 文件中；少量核心字段位于顶层，复杂且可选的元数据按职责嵌套分组，不为每篇文章配套独立 sidecar 文件。该决定不确认具体顶层字段、分组名称、字段类型、必填性、默认值或校验规则。
- **D-038 / 2026-07-15（值与框架映射语义已被 D-058 部分替代）**：技术文章的 `slug` 是必填的 frontmatter 顶层字段，也是 `/writing/<article-slug>/` 的唯一 URL 真相源；文件名和目录只服务仓库组织，不生成或覆盖公开 URL。构建门禁必须检查 D-035 规定的格式、全站唯一性，并在已发布 slug 变更时要求永久重定向记录。D-058 保留本条的真相源、稳定性和文件路径解耦要求，但将字段值明确为 Docusaurus 原生根相对完整路径，不再从短值派生栏目路径。
- **D-039 / 2026-07-15**：技术文章使用必填的 frontmatter 顶层字段 `publicationStatus` 表示发布可见性；不使用含义较宽的 `status`，也不沿用候选名 `stage`。该决定只确认字段名、顶层位置、必填性和职责，允许值以及 `planned` 是否进入文章集合仍需另行确认。
- **D-040 / 2026-07-15**：技术文章 `publicationStatus` 只允许 `draft`、`published`、`archived`。生产构建不生成 `draft` 的列表项、正文路由或 sitemap 条目；`published` 正常公开；`archived` 保留公开正文并明确标注不再维护。`planned` 只存在于路线或选题记录，不进入文章集合，不创建文章文件、文章 slug 或文章 URL。
- **D-041 / 2026-07-15**：技术文章使用必填且非空的 frontmatter 顶层字段 `authors`，其值是站内作者 ID 列表，并引用 Git 管理的作者注册表。作者身份不等于未来中央账户，也不授予内容编辑或发布权限；该决定不自动创建作者详情页。作者注册表路径、ID 规则、资料字段和是否公开作者路由仍需另行确认。
- **D-042 / 2026-07-15**：作者注册表采用 Git 管理的单一 JSON 对象，稳定作者 ID 是对象键，作者资料是对应值；当前不使用每位作者一个数据文件或 Markdown 档案。该决定不确认注册表路径、本站作者 ID、资料字段、ID 格式校验或作者公开路由。
- **D-043 / 2026-07-15**：本站首个稳定作者 ID 为 `lyty1997`。个人作者、Axial Muse 站点品牌、未来发布组织与未来中央账户是四个独立身份边界；作者 ID 不进入文章 URL，不随 GitHub 用户名或公开显示名变化。GitHub 地址和显示名属于可更新资料字段，未来商业化不要求改写历史文章作者引用。
- **D-044 / 2026-07-15**：作者注册表使用必填字符串字段 `displayName` 作为公开署名；首个作者 `lyty1997` 的初始 `displayName` 同为 `lyty1997`。显示名可以在保持作者 ID 不变的情况下更新，因此未来改用个人姓名或长期笔名不需要修改文章引用或 URL。
- **D-045 / 2026-07-15**：作者注册表首版资料只包含必填 `displayName` 与嵌套的 `links.github`；首个作者的 GitHub 地址为 `https://github.com/lyty1997`。首版不加入 `bio`、`avatar` 或其他社交链接。是否要求未来每位作者都提供 GitHub，以及注册表路径和 URL 校验细节仍需另行确认。
- **D-046 / 2026-07-15**：技术文章使用必填的顶层纯文本字段 `title` 和 `summary`。`summary` 是目录摘要、正文导语、默认 meta description 和默认分享描述的单一真相源，不增加 `excerpt`，也不从正文自动截取。可选 `seo.description` 覆盖 meta description，可选 `seo.socialDescription` 按“自身 -> `seo.description` -> `summary`”回退；空白覆盖、与回退值完全相同的冗余覆盖或页面硬编码摘要必须由机器门禁拒绝。新增覆盖需在评审中说明原因，正文、标题或摘要发生实质修改时必须人工复核已有覆盖。摘要精确长度限制仍需在 SEO 契约中确认。
- **D-047 / 2026-07-15**：技术文章日期使用 frontmatter 顶层 `publishedAt` 与 `updatedAt`，格式为 `YYYY-MM-DD`。`draft` 禁止填写 `publishedAt`，可选 `updatedAt` 且不公开；`published` 和 `archived` 必须同时填写两个日期，首次发布时相同，`publishedAt` 此后不可修改。发布辅助命令在作者操作阶段按 `Asia/Shanghai` 写入并持久化到 Git；公开文章发生可见修改时同步更新 `updatedAt`。CI、静态站点构建和生产服务器只能读取与校验，不能注入或改写日期；迁移旧文允许在提交前手工提供真实日期。命令名称、实现和跨版本差异检查方式仍需在构建发布契约中确认。
- **D-048 / 2026-07-15**：技术文章不使用通用主分类，改按“项目 -> 模块 -> 主题标签”组织目录与发现关系；这些组织维度不进入文章 URL。项目与模块是否可空、单篇文章的项目/模块基数、模块是否严格隶属项目、非项目文章的目录位置，以及主题标签数量和词表仍需另行确认。
- **D-049 / 2026-07-15**：技术文章使用单一规范组织归属：`project` 可选且最多一个；`module` 可选且最多一个，只有已选择 `project` 时才允许填写，并且必须隶属该项目。填写项目但不填写模块的文章归入该项目根级；两者均不填写的文章归入独立的通用技术分组，其公开名称仍需另行确认。`topics` 必填 1-5 个，引用受控主题 ID。跨项目文章只选择一个主项目，其他项目仅作为相关关系，不在多个项目侧栏重复出现。项目、模块、主题均使用稳定 ID，显示名称来自 Git 注册表；重分类不改变文章 URL。frontmatter 分组名称、各注册表结构与路径、通用分组名称、跨项目相关关系字段仍需另行确认。
- **D-050 / 2026-07-15**：技术文章使用必填的 frontmatter 对象 `classification` 作为项目、模块和主题组织信息的唯一真相源。`classification.project` 是可选的单个项目 ID；`classification.module` 是可选的单个模块 ID，并继续受 D-049 的项目依赖与归属约束；`classification.topics` 是必填的 1-5 个受控主题 ID 列表。不得在 frontmatter 顶层，或在 `relations`、`taxonomy` 等其他分组中为这三个组织字段建立别名或副本。`classification` 只是元数据职责分组，不重新引入通用主分类，也不进入或改变文章 URL。项目/模块/主题注册表结构与路径、通用分组名称、跨项目相关关系字段和系列结构仍需另行确认。
- **D-051 / 2026-07-15**：主站静态站点生成器改选 Docusaurus，替代 D-029 的 Astro 目标。选择依据是用户已有其他 React 项目，希望主站复用 React 工程栈与 Docusaurus 成熟的文档能力；不把“更先进”记录为可验证的客观结论。Docusaurus 仍只在构建阶段运行，生成结果由 Nginx 静态提供，生产请求不运行 Docusaurus 或 Node.js 服务。选择 Docusaurus 包含其标准静态页面所需的 React 客户端资源，但不批准自定义客户端组件或第三方脚本。本决定不同时批准具体版本、preset、docs/blog 插件拓扑、主题覆盖、构建命令或产物目录。
- **D-052 / 2026-07-15**：采用开源依赖分层准入。主站构建依赖、前端依赖图和会进入浏览器静态产物的新增软件优先选择 MIT、Apache-2.0、BSD-2-Clause、BSD-3-Clause 或 ISC；“优先”只用于筛选候选，不自动批准具体包、版本或传递依赖。MPL、LGPL 等非首选许可逐项核验；GPL、AGPL 等强 copyleft，以及自定义、source-available、双重授权或义务不清的复杂许可，不得直接链接、编译或复制进主站浏览器包和静态产物，确有需要时只能作为具有独立部署与 API/网络边界的候选服务，并另行评估许可、安全、数据和运维影响，服务隔离不免除其许可证义务。社区插件、SDK、iframe、分析、登录、评论、搜索，以及任何浏览器离开本站 origin 的请求，不论许可证为何均须单独决策。开发、构建与运维工具按自身使用和分发方式独立登记；字体、图标、图片、视频、外部文档和商标按各自许可处理。该政策不批准 Docusaurus 的具体版本、preset、插件、Pagefind、CMS、身份、评论、分析、监控、组件库、外部服务、境外请求、数据采集、部署方式或任何新增 npm 包，也不决定本站源码与文章内容采用何种许可证。
- **D-053 / 2026-07-15**：首版工程技术基线固定为 Docusaurus 官方静态构建能力、仓库现有 PlantUML 源码编译为静态 SVG 的图表链路、Ubuntu Server 24.04 LTS 上的 Nginx/Certbot 与 systemd/logrotate 原生运维、GitHub Actions 经最小权限 CAM 调用固定 TAT command 的发布控制链，以及 CI 质量与供应链门禁。门禁能力必须覆盖可重复依赖安装与构建、lint/typecheck/测试、代码与内容模型、链接/资源/路由/SEO/草稿隔离、PlantUML 编译、许可证准入（未知或未获批即失败）与第三方声明或 SBOM、漏洞与 Secret、构建制品外部请求、真实浏览器与可访问性、基于真实制品的 CSP，以及发布后 HTTPS/页面/资源冒烟；任一发布必需门禁失败时不得把 release 标记为成功。生产只提供静态制品，不运行 Docusaurus 或 Node.js 服务。使用“官方能力”表示优先由 Docusaurus 官方且通过后续依赖准入的能力解决文档站需求，不自动批准每个官方 preset、plugin 或可选功能。本决定固定组件职责、运行边界和门禁类别，不批准具体版本、包管理器、lockfile 类型、preset/plugin 拓扑、新增 npm 包或 Action、扫描器、schema/清单/SBOM 格式、阈值、例外流程、构建位置、命令和产物目录，也不引入 CMS、站内搜索、运行时主站后端、数据库、容器、分析、登录、评论或第三方浏览器请求。
- **D-054 / 2026-07-15（`routeBasePath` 已由 D-058 补充，内容根已由 D-061 补充）**：Docusaurus 首版内容拓扑采用单一 docs 内容实例，项目介绍与技术文章由同一实例承载，但分别使用项目侧栏和技术分享侧栏；不启用 blog 内容实例，也不配置第二个 docs 内容实例。单一实例只统一内容插件与构建边界，不把项目与文章合并为同一种领域模型，不改变 `/projects/`、`/projects/<project-slug>/`、`/writing/`、`/writing/<article-slug>/` 的既定职责，也不形成独立发布或故障隔离。首版不因框架能力自动生成时间流、归档、Feed、作者页、主题页或其他未批准路由。D-058 将该单一实例的 `routeBasePath` 固定为 `/`，D-061 将仓库根 `site-content/` 固定为物理内容根；具体版本、preset、内容根内除已确认文章边界外的来源与位置、侧栏文件与键名、侧栏生成和排序、列表页生成方式、其余字段映射、主题适配及版本化配置仍需后续确认。未来若出现相互独立的文档版本生命周期，或明确批准时间流、归档、Feed 等产品需求，应重新评估多个 docs 实例或 docs + blog；其他重新评估情形同样必须基于真实需求并经用户确认，迁移必须保持既定公开 URL。
- **D-055 / 2026-07-16（技术文章核心字段映射由 D-059 补充，类型边界由 D-060 补充）**：技术文章已确认的领域内容模型及其引用的 Git 注册表是唯一可编辑真相源；Docusaurus 所需的等价或派生元数据只能在构建期只读直传或生成，不回写源内容，也不在已提交文件中持久化 `draft`、`description`、`tags`、`date` 或第二套作者资料等语义副本。该方向同样约束后续经用户确认的项目领域来源，但不决定项目介绍继续由 `projects.json` 生成，还是迁移为 Markdown/MDX 内容。D-059 后续补充技术文章核心字段的直传、派生与不映射规则，D-060 补充技术文章唯一成员边界；完整 schema、项目字段适配与契约测试仍需后续确认。本决定不批准具体 Docusaurus 配置或依赖，也不启用作者页、主题页或其他未批准路由。
- **D-056 / 2026-07-16（经 D-058 重新开放，由 D-059 以最小职责重新确认）**：技术文章领域 frontmatter 到 Docusaurus 内存元数据的构建期适配采用官方全局 `markdown.parseFrontMatter` 扩展点，并在调用 `defaultParseFrontMatter` 后把解析结果交给仓库内的纯投影函数；投影只返回本次构建使用的内存结果，不修改文章正文或源 frontmatter，不回写文件，也不生成第二棵临时内容目录。该决定形成时尚未逐字段核对 Docusaurus docs 原生 frontmatter 能力。D-058 确认 `slug` 可以原生直传后，用户要求把受影响的自定义适配重新决策，因此本条曾暂停实施授权。D-059 完成核心字段 fit-gap 后重新确认相同的官方执行点和无副作用边界，但把投影职责缩小为两个必要派生；当前字段契约与实施门禁以 D-059 为准。
- **D-057 / 2026-07-16（经 D-058 重新开放，核心方案由 D-060 独立重新确认）**：未来单一 Docusaurus docs 内容根内的 `writing/` 子树是技术文章内容类型的唯一判据。全局 `markdown.parseFrontMatter` 只对规范化后确认位于该边界内的 Markdown/MDX 调用技术文章纯投影函数；边界内的每个候选文件都必须按技术文章领域 schema 校验，缺失或无效字段必须使构建失败，不能退回普通 Docusaurus 文档。边界外文件保留 `defaultParseFrontMatter` 结果，不自动解释为项目内容，也不通过新增 `contentType` 字段或独立文章成员清单建立第二种类型判据。`writing/` 只决定内容类型，不生成或覆盖公开 URL、slug、侧栏归属或排序；文章 URL 继续只由 D-038 的源 `slug` 形成 `/writing/<article-slug>/`。这里的内容根不是仓库现有的设计文档 `docs/` 目录。由于本条把类型边界与 D-056 的全局适配执行点绑定，D-058 后按用户要求整体重新评审并暂停实施；D-060 后续重新确认核心目录判据并解除该绑定，当前授权以 D-060 为准。
- **D-058 / 2026-07-16**：技术文章直接在源 frontmatter 顶层保存 Docusaurus 原生根相对完整路径，例如 `slug: /writing/dependency-inversion`；单一 docs 实例使用 `routeBasePath: '/'`，框架默认解析结果中的 `slug` 原样参与路由，不再由适配器把短文章标识投影为 `/writing/<article-slug>`。本决定保留 D-031 的公开路由职责，部分替代 D-035 的文章完整字段字符语义和 D-038 的值及映射语义，补充 D-054 的实例路由基址，并继续遵守 D-055 的单一可编辑真相源：原生 `slug` 同时是领域 URL 契约与框架字段，不产生第二份元数据。项目注册表中的 `project-slug` 仍是短标识；项目介绍是否直接保存原生 `/projects/<project-slug>` 必须随项目内容来源另行决定。本决定形成时把 D-056 与 D-057 一并重新开放评审；D-059 后续重新确认 D-056 的最小适配职责，D-060 后续独立重新确认 D-057 的核心目录判据。本决定形成时尚未确定站点级尾斜杠、完整 slug 与保留路径校验、全站路由冲突、项目内容来源、完整 schema 和错误契约；E-001/E-002 及后续编码 Spec 已补充这些方向，OD-014 与 #5 至 #14 后续完成了具体实施设计收口并继续跟踪实现。本决定本身不授权修改配置、代码或依赖。
- **D-059 / 2026-07-16**：技术文章采用“Docusaurus 原生字段精确直用 + 必要字段只读内存最小适配”。默认 frontmatter 解析后的顶层 `title` 与 D-058 规定的完整 `slug` 原样交给 docs 插件，不重命名、规范化或生成副本；本站 schema 仍必须拒绝缺失或非法值，不能接受框架 fallback 代替必填字段。全局 `markdown.parseFrontMatter` 调用一次 `defaultParseFrontMatter` 后，仅对已经由后续经用户确认的内容类型判据识别为技术文章的文件调用仓库内纯投影函数：内存 `description` 始终由源 `summary` 派生，只有 `publicationStatus: draft` 派生原生 `draft: true`，`published` 与 `archived` 不映射为 `unlisted`。原生 `description` 是所有 Docusaurus 原生消费者共享的默认摘要，不是 SEO 覆盖后的最终页面描述；`seo.description` 与 `seo.socialDescription` 不参与该派生，后续页面元数据必须分别实现 D-046 的回退契约，并保证目录摘要继续使用 `summary`。`authors`、`publishedAt`、`updatedAt` 与 `classification` 保持领域字段，不映射到 blog 作者、`last_update` 或原生 `tags`；首版不启用 `unlisted`、原生标签路由或作者页。源文件不得双写派生的 `description`、`draft` 或其他语义副本。D-059 重新确认 D-056 的官方执行点与无文件写入、无临时内容树边界，但不决定内容类型判据；D-060 后续为技术文章提供唯一成员边界。D-060 不解除其余实施门禁，本决定仍不批准 Docusaurus/Node.js 版本、preset、依赖、配置、函数 API、schema 格式、错误契约、项目字段适配、归档展示、页面 SEO 组件、主题覆盖、侧栏生成、构建命令或发布方式。
- **D-060 / 2026-07-16**：相对未来单一 Docusaurus docs 内容根，规范化后确实位于 `writing/` 子树内的 Markdown/MDX 是唯一技术文章成员集合；这里的内容根不是仓库现有的设计文档目录 `docs/`，本决定不选择其物理路径。`writing/` 根级及任意深度子目录中的候选文件都必须先按技术文章领域 schema 校验，再应用 D-059 的最小内存投影；字段缺失、枚举非法、注册表引用或跨字段约束失败时必须中止构建，不能退回普通 doc，也不能因 Docusaurus 的 include/exclude 或 partial 规则绕过本站文章校验。边界外文件只确定为“非技术文章”：不运行文章 schema 或投影，也不自动解释为项目介绍、首页或其他内容类型。不得新增 `contentType`、独立文章成员清单或其他并行判据，也不得使用字段存在性、`slug`/URL 前缀、侧栏成员、文件名、doc ID 或分类关系反向推断类型。`writing/` 只决定内容类型，不生成或覆盖 D-058 的完整 `slug`、公开 URL、canonical、分类、侧栏归属或排序；schema、D-059 投影和未来经批准的消费者必须复用同一判型结果，不能各自实现漂移规则。本决定只确认相对类型边界，不批准创建内容根或 `writing/` 目录，也不决定物理内容根、`writing/` 内部组织、路径规范化与精确包含算法、符号链接和大小写策略、完整 schema、未知字段策略、函数 API、错误契约、测试实现、项目内容来源、项目类型判据、Docusaurus 配置或任何代码。
- **D-061 / 2026-07-16**：单一 Docusaurus docs 内容实例的仓库相对物理内容根固定为根级 `site-content/`。D-060 的相对文章类型边界因此锚定为 `site-content/writing/`：该子树根级及任意深度中的 Markdown/MDX 都继续受 D-060 的失败关闭校验与唯一判型规则约束。现有 `docs/` 继续作为内部设计真相源，迁移前 `public/` 继续只描述当前手写发布结果，二者都不是目标公开内容根。物理路径不生成或覆盖文章 `slug`、公开 URL、canonical、分类、侧栏归属或排序，也不把 `site-content/` 中 `writing/` 之外的文件自动解释为项目介绍、首页或其他内容类型。本决定只确认仓库根目录名和既有文章边界的物理锚点，不批准创建目录、修改 Docusaurus 配置或引入依赖；不决定 `writing/` 内部组织、文件命名、路径规范化与精确包含算法、误放检测、符号链接与大小写策略、include/exclude/partial 实现、Docusaurus 应用根、项目内容来源或 `site-content/projects/`、首页与列表页来源、schema、函数 API、错误契约、侧栏、主题、SEO、构建或发布方式。
- **D-062 / 2026-07-16**：技术文章采用“每篇文章一个独立源码目录”的物理布局。每篇文章必须位于 `site-content/writing/<source-name>/` 这一直接子目录中，并且该目录恰好包含一个正文入口：`index.md` 或 `index.mdx`，二者不得同时存在。D-060 的宽候选边界继续负责发现 `site-content/writing/` 任意深度的 Markdown/MDX；根级 Markdown/MDX、非 `index` 正文、同目录双入口或额外 Markdown/MDX 不能因不符合布局而被忽略，必须由后续路径与 schema 门禁失败关闭。`<source-name>` 是稳定的仓库组织名；本站不从它生成、覆盖或校验文章 `slug`、公开 URL、canonical、`classification`、显式 doc ID、侧栏归属或排序，标题、项目或模块归属变化也不要求移动源码目录。正文 frontmatter 与正文继续保存在唯一入口文件中，不引入元数据 sidecar。Docusaurus 官方文档说明文件路径会影响默认 doc ID、`index` 具有目录索引约定，而显式 `slug` 可独立控制 URL；本站只利用物理布局做源码组织，不把框架路径行为提升为领域真相源。本决定不批准创建目录、内容或配置，也不决定 `<source-name>` 字符与生成规则、目录重命名流程、路径规范化与包含算法、符号链接和大小写策略、误放错误格式、文章素材共置或共享规则、允许的非 Markdown 文件、MDX 组件位置、PlantUML 的 MDX 支持、doc ID 稳定契约、侧栏生成、schema、函数 API、测试实现、项目内容布局、构建或发布方式。官方依据：[Create a doc](https://docusaurus.io/docs/create-doc)。
- **D-063 / 2026-07-16**：技术文章的 `<source-name>` 采用作者手工确定、独立、稳定且可读的语义源码名，只为在 Git、PR 和编辑器中辨识 `site-content/writing/<source-name>/` 文章目录，不是文章领域身份。它是 `site-content/writing/` 下的单个直接子目录路径段，长度必须为 1-64 个 ASCII 字符并完整匹配 `^[a-z0-9]+(?:-[a-z0-9]+)*$`：只允许小写 ASCII 字母、数字和分隔非空片段的单个连字符，不允许大写字母、非 ASCII 字符、空白、下划线、点、斜杠、前导或尾随连字符及连续连字符；在当前 `site-content/writing/` 文章直接子目录命名空间内必须全局唯一。作者创建文章时显式提供该名称；未来工具可以接收并校验作者输入，但不得从 `title`、完整 `slug`、`classification`、日期、正文或其他字段自动生成、同步改写或静默纠正。`<source-name>` 可以恰好与文章 slug 尾段相同，但不形成等值、派生或持续同步契约；`slug` 继续是公开 URL 的唯一真相源，`classification` 继续是项目、模块和主题组织的唯一真相源，标题、slug、项目、模块、主题或日期变化均不自动移动源码目录。首次发布前允许在一次受审变更中受控改名：原子移动完整文章目录，不复制第二个正文入口，同步更新所有旧文件路径和旧 doc ID 消费者，并通过完整静态构建、内部链接与资源断链检查；目录改名不得自动改变 slug。文章首次发布后禁止日常改名；只有拼写错误或持续造成严重歧义时，才可在用户对该次操作明确授权后作为独立迁移处理，以 `git mv` 形式完成完整目录移动，保持公开 slug 与 canonical URL 不变，原子更新全部路径和 doc ID 消费者并通过同等门禁。Docusaurus 默认 doc ID 仍随父目录路径变化，D-058 的根相对完整 `slug` 保持公开 URL 不变；因此受控改名必须检查侧栏或其他框架消费者及入站 Markdown/MDX 相对文件链接。本站尚未选择的 Docusaurus 版本必须用契约测试复核这些行为。本决定不批准创建目录、内容、配置或代码，也不决定保留名、旧名称复用、路径规范化与真实包含算法、符号链接和文件系统大小写处理、历史状态检测实现、显式 doc ID 与文章间链接及侧栏引用策略、素材布局、允许的非 Markdown 文件、误放错误格式、schema、函数 API、测试工具、Docusaurus 配置、构建或发布方式。官方依据：[Create a doc](https://docusaurus.io/docs/create-doc)、[Markdown links](https://docusaurus.io/docs/markdown-features/links)。
- **D-064 / 2026-07-17**：技术文章新增必填顶层字段 `articleId`，作为跨源码路径、公开 URL 与未来服务边界的稳定文章身份。`articleId` 必须是符合 [RFC 9562 UUIDv7](https://www.rfc-editor.org/rfc/rfc9562.html#section-5.7) 的全站唯一值，自文章创建时分配后终身不可修改、不得转给或复用于另一篇文章；标题、正文、`<source-name>`、`slug`、`classification`、发布状态与日期变化均不改变它。它不进入公开 URL，也不生成、覆盖或校验 Docusaurus doc ID、源码目录、slug、canonical、分类或侧栏。UUIDv7 时间字段只记录生成器在分配 ID 时采用的 Unix 毫秒时间源值，可服务 UUID 值的未来存储索引局部性与技术排序，但不保证真实业务事件顺序，也不是文章 `createdAt`、`publishedAt` 或 `updatedAt`；当前不新增 `createdAt`，不得从 UUID 解码、补齐或校验公开日期，也不得用它驱动 SEO、sitemap、公开列表、侧栏、归档或日期搜索。D-047 的 `publishedAt` 与 `updatedAt` 继续分别作为首次公开日期与最近公开修订日期的唯一真相源。每篇文章唯一的 `index.md` 或 `index.mdx` 同时保存 `articleId`、slug、发布状态和显式日期，是这些字段与正文之间的唯一可编辑绑定；未来构建可以从通过 schema 的源内容只读派生记录仅包含 `articleId`、当前 `slug`、`publishedAt` 与 `updatedAt` 的查找索引，本决定不批准索引记录包含其他字段；该索引必须可重建、不得回写文章、不得由作者手工编辑或作为第二份绑定提交。索引的格式、位置、draft 边界、消费者、缓存与公开范围仍需后续决定，本条不批准站内搜索、搜索 UI、插件、外部搜索服务或查询数据采集。Docusaurus 内部不把 `articleId` 映射为原生 `id`：首版文章不填写原生 frontmatter `id`，默认 doc ID 只作为当前构建的框架内部引用；本站不得手工拼接或提交 doc ID 可编辑映射，也不得将其作为跨构建、跨发布或领域服务的持久身份。Docusaurus 在当前临时目录与静态构建制品中的框架内序列化不构成本站真相源。正文中的同实例文章链接统一使用带实际 `.md` 或 `.mdx` 扩展名的源码相对文件路径，不使用 articleId 自定义协议、doc ID 或硬编码公开 slug；Docusaurus 将其转换为目标 permalink，源码目录或扩展名变更时必须原子更新入站链接并通过断链检查。技术分享侧栏由官方 `sidebarItemsGenerator` 在构建期读取已校验的文章成员、`publicationStatus`、`classification` 和插件提供的当前 `docs[].id`，生成 `type: 'doc'` 项；生产只为 `published` 与 `archived` 生成侧栏项，`draft` 不进入生产侧栏，不得用框架默认行为替代本站可见性门禁；不得按物理目录、articleId、slug 或自建成员清单反推分类，不手写或提交第二份 doc ID 成员清单。该决定只固定侧栏身份引用、可见性和真相源方向，不决定开发预览中的 draft 侧栏、侧栏键名、注册表显示、通用分组名称、排序、折叠、分页、函数 API、错误格式或测试实现。本条形成时不决定 UUIDv7 生成库与命令、文本规范、同毫秒单调与时钟回退策略、历史不可复用检查、旧文迁移、结构化相关文章或系列引用、派生索引实现、Docusaurus 版本、依赖、构建或发布方式；D-065 后续补充生成责任和作者入口，当时具体接口与后端仍未决；D-066 再固定 Node 24 LTS 基线与原生 `randomUUIDv7()` 后端，命令接口仍未决。本条不批准创建内容、索引、schema、配置或代码；选定版本后必须用契约测试复核默认 doc ID、相对文件链接和 `sidebarItemsGenerator` 行为。UUID 不是秘密或授权凭证；未来评论若获批准，其文章外键使用 `articleId`，但评论、账户、API、数据库及用户数据处理仍需独立决策。官方依据：[RFC 9562](https://www.rfc-editor.org/rfc/rfc9562.html)、[Create a doc](https://docusaurus.io/docs/create-doc)、[Markdown links](https://docusaurus.io/docs/markdown-features/links)、[`sidebarItemsGenerator`](https://docusaurus.io/docs/api/plugins/@docusaurus/plugin-content-docs#sidebargenerator)。
- **D-065 / 2026-07-17**：新技术文章的正常创建入口固定为作者显式运行的仓库内 Node.js 文章创建命令。命令在一次作者触发的创建操作中建立尚不存在的 `site-content/writing/<source-name>/` 目录及 D-062 规定的唯一 `index.md` 或 `index.mdx` 正文入口，并为该入口生成一个符合 D-064 的 UUIDv7，作为顶层 `articleId` 写入一次；成功结果必须同时具备新目录、唯一正文入口和 articleId，失败必须恢复到调用前状态，不得留下目标文章目录、正文入口或本次创建产生的其他持久化结果。作者必须按 D-063 显式确定 `<source-name>`；命令不得从 articleId、标题、slug、`classification`、日期、正文或其他字段相互推导、同步改写或静默纠正这些领域值。命令只把结果写入作者工作区供其审查 Git diff，不自动暂存、提交、推送或发布；既有文章不得借普通创建入口覆盖、轮换、修复或补写 articleId，旧文分配继续属于独立迁移。Git hook、PR bot、CI、Docusaurus 构建、预览、发布自动化和生产服务器均不得触发创建命令或生成、修复 articleId，只能对源内容做只读校验或派生已批准的构建结果。D-047 的发布日期辅助命令保持独立：文章创建命令不得从 UUID 时间写入 `publishedAt` 或 `updatedAt`，也不得自动发布文章。本决定只确认生成责任、显式作者入口、一次性分配时点和可观察的完整成功或失败边界，不批准创建实际目录、文章、命令、配置或代码；不决定命令名称与路径、参数或交互、`index.md`/`index.mdx` 选择方式、模板与其他字段输入、文件系统原子实现、UUID 文本规范与生成后端、Node.js 版本、npm 包或 lockfile、同毫秒单调、时钟回退、当前树及 Git 历史查重实现、旧文迁移、schema、错误格式或契约测试。
- **D-066 / 2026-07-17**：主站首版作者工作区、仓库质量门禁、CI 与 Docusaurus 静态构建统一采用 Node.js 24 LTS，最低受支持版本为 24.16.0，等价范围为 `>=24.16.0 <25`；后续主版本必须重新确认。D-065 文章创建命令固定使用 Node.js `node:crypto` 原生 `randomUUIDv7()`，不引入 UUID 生成 npm 包、系统 CLI 或仓库外服务。本站接受原生 API 在同毫秒、时钟回退、跨进程或跨机器时不保证严格递增，不增加计数器、共享状态或时间修正包装；articleId 不承担业务顺序或文章日期，唯一性、历史不可复用与未授权改写仍由独立门禁负责。目标质量和行为契约只在 Linux 作者环境与 Ubuntu CI 验证。当前 Node 22、`package.json` 与 workflow 是迁移前事实，生产继续由 Nginx 提供静态制品。本决定不授权立即修改环境、代码、依赖、CI、命令或内容；Docusaurus 精确版本、`disableEntropyCache`、UUID 文本与 schema、历史检查、旧文迁移、错误格式和测试实现仍需后续确认。官方依据：[Node.js `crypto.randomUUIDv7()`](https://nodejs.org/docs/latest-v24.x/api/crypto.html#cryptorandomuuidv7options)、[Node.js Release Working Group](https://github.com/nodejs/release#release-schedule)、[Docusaurus installation](https://docusaurus.io/docs/installation)。
- **D-067 / 2026-07-17**：Node 24 的正常执行基线采用仓库根 `.nvmrc` 中的单个精确 patch 作为唯一可编辑真相源，初始值固定为 `24.18.0`；`package.json#engines.node` 独立表达 `>=24.16.0 <25` 兼容边界。Linux 作者环境通过 nvm 读取 `.nvmrc`；Ubuntu CI 的主质量、PlantUML 与未来正式 Docusaurus 构建通过 `actions/setup-node` 的 `node-version-file` 读取同一值，并先断言实际版本精确匹配。Ubuntu CI 另设只用于兼容验证的最低版本入口：断言 Node 为 `24.16.0` 后调用与正常入口相同的质量、构建和行为测试负载，不产出发布制品，也不触发文章创建或发布。版本契约同时检查 `.nvmrc` 是兼容范围内的非浮动精确版本，最低入口等于范围下界；不得提供通用版本检查绕过开关。安全 patch 及时走独立 PR，其他 patch 至少每月检查；升级 PR 必须让 Ubuntu 主任务、最低版本任务、PlantUML 和发布必需门禁全部通过，且不得自动合并或在普通 patch PR 中修改 `engines` 边界。目标质量、构建和行为契约只在 Linux 作者环境与 Ubuntu CI 验证。生产服务器不安装或运行 Node，当前 Node 22 实现尚未迁移。Ubuntu nvm 的精确版本与获取校验、`actions/setup-node` 固定版本与 commit SHA、两个入口和 job 拓扑、npm、lockfile 及迁移步骤仍需后续确认。官方依据：[Node.js 24.18.0](https://nodejs.org/en/blog/release/v24.18.0)、[`actions/setup-node` 版本文件](https://github.com/actions/setup-node#usage)、[nvm `.nvmrc`](https://github.com/nvm-sh/nvm#nvmrc)。
- **D-068 至 D-071 / 2026-07-17（已由 D-072 撤销）**：曾探索非 Linux 作者执行环境及其本地工具供应链；该路线未实施，且不再构成现行架构、待办或维护义务。完整过程由 Git 历史和[项目进度](../progress.md)留存。
- **D-072 / 2026-07-17**：本站执行平台采用单一 Linux/Ubuntu 路线。作者 Node.js、文章创建与日期辅助命令、仓库质量检查和 Docusaurus 构建只在 Linux 作者/预览环境运行，GitHub Actions 只使用 Ubuntu；Linux 本地 hook 只提供快捷反馈，不能代替 CI。生产服务器只提供经过验证的静态制品，不运行 Node.js 请求服务。其他客户端只承担不依赖本站 Node.js 工具链的协作操作，具体边界由[开发预览工作流](dev-workflow.md)维护。D-066 的 Node 24 LTS 范围、原生 `randomUUIDv7()` 后端和 D-067 的 `.nvmrc` 精确基线、`engines.node` 兼容边界、最低端点验证与受控 patch 升级治理继续有效。
- **D-073 / 2026-07-17**：主站首个 Docusaurus 依赖基线固定为 `3.10.2`，使用同版本的 `@docusaurus/core`、`@docusaurus/preset-classic` 与 `@docusaurus/faster`；配置启用 `future.v4: true`，提前采用 v4 兼容行为与已经稳定的 Faster 构建链。classic preset 中的 docs、pages、classic theme 与 sitemap 服务本站既定静态内容职责；必须显式设置 `blog: false`，不配置 Algolia、Google Analytics、Google Tag Manager、旧 Google Analytics 插件或其他浏览器外部请求。包管理器采用 Node.js 随附的 npm，仓库只提交一个 `package-lock.json`，不并存其他包管理器 lockfile；正常 Linux 作者入口与主 Ubuntu CI 使用 D-067 的 Node `24.18.0` 及其随附 npm `11.16.0`，只有受审依赖变更可以在该基线更新依赖清单与 lockfile，其余正常验证、CI 与构建入口统一使用 `npm ci`。最低兼容入口使用 Node `24.16.0` 及其随附 npm `11.13.0`，只读取同一 lockfile 并运行 `npm ci` 与共享负载，不更新 lockfile、不生成发布制品；两个 npm 版本读取同一 lockfile 的兼容性必须在首次迁移时失败关闭验证，不通过时回到决策流程，不得静默改用浮动 npm 或第二种 lockfile。本决定批准上述三个 Docusaurus 官方包、preset 拓扑、future flags、包管理器与冻结安装方向，但不批准 community plugin、搜索、分析、第三方脚本、浏览器外部请求、React/React DOM/MDX 等其余直接依赖的具体版本，也不豁免 D-052 的逐项直接与传递依赖许可证、NOTICE、漏洞、生命周期脚本、SBOM/第三方声明和构建制品网络检查。当前只记录设计，不安装依赖、不创建 lockfile、配置或工程骨架；实际迁移必须等待其余直接依赖清单、准入工具与构建发布契约确认。官方依据：[Docusaurus 3.10.2](https://github.com/facebook/docusaurus/releases/tag/v3.10.2)、[Docusaurus 3.10 与 v4 future flags](https://docusaurus.io/blog/releases/3.10)、[Docusaurus 安装与同版本要求](https://docusaurus.io/docs/installation)、[classic preset 组成](https://docusaurus.io/docs/using-plugins#docusauruspreset-classic)、[`npm ci`](https://docs.npmjs.com/cli/commands/npm-ci/)、[Node.js 24.18.0](https://nodejs.org/en/blog/release/v24.18.0)、[Node.js 24.16.0](https://nodejs.org/en/blog/release/v24.16.0)。
- **D-074 / 2026-07-17**：Docusaurus 管理的目标站点源码统一采用严格 TypeScript：站点配置使用 `docusaurus.config.ts`，侧栏配置与生成器使用 `sidebars.ts` 及其 `.ts` 模块，本地插件、构建期领域适配和无 JSX 的站点模块使用 `.ts`，页面、主题覆盖及 React 组件在包含 JSX 时使用 `.tsx`。目标 `tsconfig.json` 必须显式启用 `compilerOptions.strict: true`；上述范围不新增 `.js` 或 `.jsx`，确有例外时必须先单独说明原因、影响和验证方式并取得用户确认。类型检查与静态构建是两个独立且都必须通过的失败关闭门禁：`typecheck` 通过 `tsc --noEmit` 校验目标范围，Docusaurus build 独立验证框架加载和静态制品，任一成功都不能替代另一项。`docusaurus.config.ts`、`sidebars.ts` 及其 Node.js 侧模块不得导入浏览器 API、React 或 JSX；展示层继续遵守既有浏览器与 Node.js 边界。现有 `scripts/quality/*.mjs` 保持迁移前 ESM，不因本决定强制改写；D-065 的未来作者 CLI 也不由本决定选择 TypeScript，其语言与执行接口另行确认。本决定不批准 TypeScript、Docusaurus 类型包、React 类型包或其他直接与传递依赖的具体版本，不决定 `tsconfig` 的继承基线、module/moduleResolution、路径别名、目录公共 API、导出、命名、lint 或 formatter 配置，也不授权安装依赖、创建配置、脚手架或目标源码。Docusaurus 官方确认其 `3.10.2` 支持 TypeScript 主题组件和 `docusaurus.config.ts`，同时明确 Docusaurus 不使用项目 `tsconfig.json` 编译站点，因此 CI 必须另行运行 `tsc`；侧栏文件在 Node.js 中执行，不能使用浏览器 API、React 或 JSX。官方依据：[TypeScript Support](https://docusaurus.io/docs/typescript-support)、[Configuration](https://docusaurus.io/docs/configuration)、[Sidebar](https://docusaurus.io/docs/sidebar)。
- **D-075 / 2026-07-18**：主站目标源码采用“Docusaurus 标准入口目录 + 显式模块边界”的物理结构。框架入口保留仓库根 `docusaurus.config.ts` 与 `sidebars.ts`；领域核心、构建适配、可复用展示组件、文件路由页面和主题覆盖分别位于 `src/domain/`、`src/build/`、`src/components/`、`src/pages/` 与 `src/theme/`；未来作者工具位于 `scripts/author/`，现有质量脚本继续位于 `scripts/quality/`。`src/build/` 是受版本控制的构建期源码目录，不是 Docusaurus 静态产物目录，产物目录仍由构建发布契约决定。这些目录只映射 CODE-002 已确认的逻辑职责和依赖方向，不建立新的横向共享层，也不改变 `site-content/` 内容目录契约。跨逻辑层依赖只能从被依赖模块的显式公共入口导入，禁止绕过入口深层导入；只有出现真实跨层消费者时才创建对应 `index.ts`，不预建空入口。公共入口必须逐项写明值与类型导出，不使用递归或宽泛 `export *`；同一模块内部使用相对导入。默认导出仅用于 Docusaurus 加载器所需的框架入口，包括站点配置、侧栏文件、文件路由页面、主题覆盖和独立本地插件构造器；内部可复用模块使用具名导出，插件静态方法按 Docusaurus 契约使用具名导出。首版不增加 `@/`、`@domain` 等自定义路径别名，只允许在对应框架语义下使用 Docusaurus 官方文档定义的别名；官方别名不得用来伪装跨层深层导入。具体公共函数和类型、文件与组件命名、hook 约定、层内子目录、`tsconfig` 继承及 module/moduleResolution、主题 Swizzle、MDX 组件白名单、作者 CLI 语言与接口、边界检查工具及 CI 接线仍需分别确认或实施。本决定不批准创建上述目录、空 barrel、配置或源码，不安装依赖，也不改变服务器、DNS、发布或用户数据边界。官方依据：[Configuration](https://docusaurus.io/docs/configuration)、[Sidebar](https://docusaurus.io/docs/sidebar)、[Creating Pages](https://docusaurus.io/docs/creating-pages/)、[Plugin Method References](https://docusaurus.io/docs/api/plugin-methods)、[TypeScript Support](https://docusaurus.io/docs/typescript-support)、[Client architecture](https://docusaurus.io/docs/advanced/client)、[Versioning](https://docusaurus.io/docs/versioning)。
- **D-076 / 2026-07-18**：首轮 Docusaurus TypeScript 编译与直接依赖采用官方 `3.10.2` 基线并由本站显式收紧。D-073 的三个框架包继续精确固定为 `3.10.2`；首次候选应用直接依赖增加 `react@^19.0.0`、`react-dom@^19.0.0` 与 `@mdx-js/react@^3.0.0`，首次候选开发直接依赖增加 `@docusaurus/module-type-aliases@3.10.2`、`@docusaurus/tsconfig@3.10.2`、`@docusaurus/types@3.10.2`、`@types/react@^19.0.0` 与 `typescript@~6.0.2`。范围表达遵循 Docusaurus `3.10.2` 官方 TypeScript 模板，实际解析版本必须由 D-073 的唯一 `package-lock.json` 精确冻结；只有受审依赖变更可以改变清单或锁定结果。`clsx`、`prism-react-renderer`、`@types/node` 及模板、主题或未来源码中的其他包不因此成为新增直接依赖；出现真实直接用途时必须重新准入，它们若已存在于候选传递图中仍须接受 D-052 的传递依赖审查。目标根 `tsconfig.json` 继承 `@docusaurus/tsconfig`，并由本站显式设置 `baseUrl: "."`、`ignoreDeprecations: "6.0"`、`strict: true` 与 `allowJs: false`；首轮 `include` 只包含根 `docusaurus.config.ts`、根 `sidebars.ts`、`src/**/*.ts` 与 `src/**/*.tsx`。`module: "esnext"`、`moduleResolution: "bundler"`、`noEmit: true` 与 `skipLibCheck: true` 由精确版本的官方基线继承，本站不重复声明，也不增加自定义 `paths`；官方基线提供的 `@site/*` 只能继续按 D-075 的框架语义使用。目标 `typecheck` 入口仍显式执行 `tsc --noEmit`，不能因继承 `noEmit` 或 Docusaurus build 成功而省略。`baseUrl` 与 `ignoreDeprecations: "6.0"` 只是 Docusaurus 3.10/TypeScript 6 的受控过渡，升级到 TypeScript 7、Docusaurus 4 或改变官方基线时必须重新审查并移除不再适用的兼容项。本决定只批准上述候选清单和配置契约，不豁免 D-052 的直接与传递依赖许可证、NOTICE、漏洞、生命周期脚本、SBOM/第三方声明、锁文件双端点及制品网络检查；相关工具与构建发布契约完成前仍不安装依赖、不创建 `tsconfig.json`、lockfile、目录或源码。官方依据：[TypeScript Support](https://docusaurus.io/docs/typescript-support)、[Docusaurus 3.10.2 TypeScript template](https://github.com/facebook/docusaurus/blob/f37f9035584917a97a260b91fc2842cba4f8b94f/packages/create-docusaurus/templates/classic-typescript/package.json)、[Docusaurus 3.10.2 tsconfig template](https://github.com/facebook/docusaurus/blob/f37f9035584917a97a260b91fc2842cba4f8b94f/packages/create-docusaurus/templates/classic-typescript/tsconfig.json)、[`@docusaurus/tsconfig` 3.10.2 source](https://github.com/facebook/docusaurus/blob/f37f9035584917a97a260b91fc2842cba4f8b94f/packages/docusaurus-tsconfig/tsconfig.json)、[TypeScript 6 `baseUrl` transition](https://github.com/facebook/docusaurus/issues/11893)。
- **D-077 / 2026-07-18**：首次 npm 依赖解析与供应链准入采用“npm 原生能力 + 仓库内零第三方依赖策略脚本”的失败关闭基线，首版不把 `license-checker-rseidelsohn`、`@cyclonedx/cyclonedx-npm` 或 GitHub Dependency Review Action 作为必需工具；本决定也不授权将它们作为可选补充引入。只有 D-067/D-073 的主端点 Node `24.18.0` 与随附 npm `11.16.0` 可以在隔离目录按已批准 manifest 执行 `npm install --package-lock-only --ignore-scripts --audit=false`，生成候选唯一 `package-lock.json`；候选图只允许来自官方 npm registry 的 registry package，Git、文件、本地目录和任意远程 tarball 来源一律拒绝。零依赖策略脚本必须先校验直接清单、`lockfileVersion: 3`、来源、适用条目的 `resolved`/`integrity`、声明许可证及安装脚本标记，再在不执行包代码或生命周期脚本的临时审查区下载精确锁定 tarball，核对 tarball 完整性并只读检查实际 `package.json`、LICENSE/LICENCE/COPYING、NOTICE 与生命周期脚本内容；未知、猜测、复杂、未获批或缺少所需许可证证据的包不得准入。所有第三方安装脚本默认禁止执行；仅当无脚本安装无法完成获批构建时，才可针对精确 `name@version` 重新说明脚本内容、必要性、风险和验证并请求用户决策，不存在通用放行。许可证与脚本人工预审通过后，SBOM 使用 npm 原生能力从锁文件生成 SPDX JSON；漏洞门禁显式检查包含开发依赖的完整锁定图，`moderate`、`high`、`critical` 阻断，`low` 保留报告但不自动阻断，禁止 `npm audit fix`，registry 或审计服务不可用时失败关闭。只有上述全部证据、漏洞门禁与最终人工准入结论通过后，主端点和最低端点才可对同一 manifest、唯一 lockfile 与同一项目 npm 配置执行 `npm ci --ignore-scripts --audit=false`，并通过执行前后哈希证明 `package.json` 与 `package-lock.json` 未改变；最低端点继续只读且不产生发布制品。`package.json` 只拥有直接依赖意图，`package-lock.json` 只拥有完整解析图，准入记录只保存无法从两者推导的用途、许可澄清、脚本处置、义务与决策编号，SPDX SBOM 与 `THIRD_PARTY_NOTICES` 只作为从锁文件和锁定 tarball 生成并由漂移检查约束的派生制品，不得成为手工维护的第二依赖清单。显式 `npm audit` 会把包名和版本提交给配置的 npm registry，fallback 还可能提交完整锁定树及 npm/Node/平台/架构/环境元数据；该构建期外发已获确认，不包含站点文章、源码、访问者或账户数据，也不授权任何浏览器外部请求。D-077 只批准工具组合、顺序、阈值、数据边界和真相源职责，不表示策略脚本、准入 schema、锁文件、SBOM、NOTICE、CI 接线或实际候选图已经创建、执行或通过；首次实际解析仍须在实施改动和外部下载前单独展示准确范围并取得操作授权。官方依据：[npm install](https://docs.npmjs.com/cli/v11/commands/npm-install/)、[`package-lock.json`](https://docs.npmjs.com/cli/v11/configuring-npm/package-lock-json/)、[`npm ci`](https://docs.npmjs.com/cli/commands/npm-ci/)、[`npm sbom`](https://docs.npmjs.com/cli/commands/npm-sbom/)、[`npm audit`](https://docs.npmjs.com/cli/v11/commands/npm-audit/)、[npm `11.16.0` release](https://github.com/npm/cli/releases/tag/v11.16.0) 与 [`allowScripts` Phase 1](https://github.com/npm/cli/pull/9415)。
- **D-078 / 2026-07-18**：用户确认 M0 总体方向已足够明确，将剩余内部技术、展示与实现细节委托给 Agent 根据仓库事实、对应版本的官方资料和工程知识判断，不再逐项请求用户选择。委托范围包括项目介绍的内容来源与构建期投影、站内 canonical 与路由冲突机制、内容 schema 和 Git 注册表的物理形式、Docusaurus 官方主题适配、内部 API/命名/目录/错误格式、测试与 CI job 编排、供应链策略实现布局，以及静态制品的构建和交付技术设计。每项工程选择仍须在依赖它的代码之前写入对应设计或 Spec，说明理由、影响和可执行验证；不得改变已确认的产品定位、公开页面职责、内容可信度、静态生产边界或数据隔离。本委托不表示用户逐项选择了后续具体值，也不授权 npm 联网解析、tarball 下载、真实传递图最终准入、安装脚本或复杂许可证例外、任何新增浏览器第三方请求或用户数据处理、未来账户/评论/在线体验、费用与法律合规判断、公开业务事实和素材，亦不授权 Git 发布或服务器、DNS、证书和云资源操作。这些排除项继续按原门禁单独确认或核验。
- **D-079 / 2026-07-18**：用户确认 Issue #11 采用严格 TypeScript 测试方案，将 `@types/node@^24.0.0` 增加为首轮直接开发依赖候选，专门为 Node 24 内置模块、`node:test` 和测试执行器提供类型声明；它不得进入浏览器 bundle，也不批准新增测试框架、TypeScript loader 或运行时服务。该候选补充并部分替代 D-076 中“`@types/node` 尚无直接用途、未获直接候选授权”的结论，D-076 的根 Docusaurus TypeScript program、官方 `bundler`/`noEmit` 基线和其余依赖清单保持不变。版本范围只表达 Node 24 类型线的直接依赖意图，实际版本必须由唯一 `package-lock.json` 冻结，并与全部直接和传递依赖一起通过 D-052、D-077、E-010 与 E-011 的来源、许可证、NOTICE、脚本、漏洞、SBOM 和双端点准入；若真实候选图不能通过，必须回到依赖决策，不能依赖传递提升或手写不完整 ambient declaration 规避。本决定授权补充候选与设计，不授权联网解析、下载、安装、修改 manifest/lockfile 或接受真实依赖图。候选版本线与其传递依赖仍以 [npm 官方包版本页](https://www.npmjs.com/package/%40types/node?activeTab=versions)和未来真实 lockfile 为准。

- **D-080 / 2026-07-20**：Linux 作者机的系统 Node 与 Bash 默认 Node 保持现状，不因本站改为 Node 24；本站只在用户目录 `~/.nvm` 安装精确 nvm `v0.40.6` 与 D-067 `.nvmrc` 指定的 Node，并且不保留 `default` alias。nvm 来源固定为官方仓库 immutable release 的 annotated tag `v0.40.6`：tag object `18f62ba4e8e2148383332fb1ac8b2ff1ee21a263`，peeled commit `b6cf55f6adf3b953d0e5e00a4049444e300e3af8`；安装必须核对官方 release 状态和这两个对象，不通过 `curl | bash` 执行远程脚本。不修改 `~/.bashrc`、`~/.profile` 或其他 shell 初始化文件，因此新会话继续解析到原系统 Node；需要运行本站门禁时，仓库 pre-commit 在自己的子进程内从固定 `~/.nvm/nvm.sh` 以 `--no-use` 加载并核对 nvm 版本，严格解析根 `.nvmrc`，再以 `nvm version`/`nvm which` 定位固定版本目录并只修改该子进程 PATH，不调用会消费用户 npm `prefix` 的 `nvm use`，随后执行 E-010 唯一质量入口。缺少 nvm、nvm 版本漂移、`.nvmrc` 非法或精确 Node 尚未安装时必须在任何质量负载前失败，不联网安装、不回退系统 Node、不修改 alias，也不绕过 hook。Node 安装只允许 nvm 使用 Node.js 官方发行源并通过发行摘要校验；验收必须证明 Node/npm 为 D-067/D-073 主端点、npm CLI 信任树通过、完整隔离质量入口成功，同时从全新 Bash 会话证明默认仍是原系统 Node。该决定只处理当前 Linux 作者用户环境和本地 pre-commit，不改变系统 Node、shell 配置、其他用户、生产服务器、CI 版本拓扑、最低兼容端点、npm 全局包、站点依赖或用户数据处理；卸载可删除用户级 Node/nvm，仓库 hook 随后将按上述边界失败关闭。官方依据：[nvm v0.40.6](https://github.com/nvm-sh/nvm/releases/tag/v0.40.6)、[nvm 安装与 `--no-use`](https://github.com/nvm-sh/nvm#installing-and-updating)、[Node.js 24.18.0](https://nodejs.org/en/blog/release/v24.18.0)。
- **D-081 / 2026-07-20**：用户要求继续推进直到 Issue #21 达到完整验收标准再统一审核，因而授权本轮通过 E-010 受控入口联系官方 npm registry，生成唯一候选 lockfile、获取并复核 lock 指向的精确 tarball、运行包含开发依赖的官方 audit、生成正式供应链三制品，并下载已固定摘要的 Node.js 官方最低端点制品完成真实双端点冻结安装。该过程可能向官方 npm 服务发送包名、版本、依赖图以及 npm/Node/平台/架构元数据，但不发送站点内容、访问者、账户或评论数据；不得使用镜像、共享 cache、用户 npm 配置、代理、自定义 CA、自动修复或安装脚本，也不得在仓库根创建 `node_modules`。本授权允许按既有 D-077 schema 写入真实 `package-lock.json`、候选审查证据和正式 SBOM/evidence/NOTICE，但不等于用户已批准真实图中的任何具体许可证、生命周期脚本或最终图准入；这些不可由 Agent 推断的项目必须合并为一次脱敏审核包，由用户明确确认后才能写入正式 admissions/最终决定并进入成功 receipt。授权不包含 commit、push、GitHub Issue/PR 写操作、Action 接线、服务器、DNS、凭证或生产操作。验收必须以官方来源与 integrity 闭包、逐包证据、实际 audit、显式最终决定、主/最低端点同一 lock 的 `ci`、manifest/lock 前后摘要不变和 composite receipt 为准；任一失败都保留可复核证据并停止成功结论。
- **D-082 / 2026-07-20**：用户确认采用 #21 审核包的 bounded-completion 方案，允许把 `serialize-javascript@7.0.5` 与 `uuid@11.1.1` 作为仅作用于传递图的精确 npm overrides，分别替代当前受漏洞影响的旧 major；它们不得成为本站直接 API，也不得扩大浏览器请求、数据处理或运行时服务边界。两项 override 已由精确 tarball、父包代表调用路径和临时冻结图探针验证，但仍跨越父依赖声明的 semver major，必须由最终 lock、全量回归、实际 audit、主/最低 Node 端点冻结安装和后续 Docusaurus build 继续验证，任一门禁失败不得绕过或退回有漏洞版本。用户同时批准为 35 个具有 canonical GitHub immutable 完整许可证文件的精确 identity、11 个在精确 registry tarball 其他受控文件区段中携带完整条款的 identity 写入补充法律证据，并允许下列 12 个精确传递包采用较弱的许可证证据载体例外：`@jsonjoy.com/json-pointer@1.0.2`、`@pnpm/config.env-replace@1.1.0`、`@pnpm/network.ca-file@1.0.2`、`@swc/counter@0.1.3`、`@tybys/wasm-util@0.10.3`、`boolbase@1.0.0`、`cacheable-request@10.2.14`、`eastasianwidth@0.2.0`、`format@0.2.2`、`glob-to-regex.js@1.2.0`、`keyv@4.5.4` 与 `svg-parser@2.0.4`。12 项只接受“官方 tarball 身份、integrity 与许可证元数据可验证，但该精确发布没有同等级的独立完整法律文件”这一残余风险；`format@0.2.2` 的 MIT 结论来自 legacy `licenses[].type`，标准许可证文本不能证明发布者遗漏的版权、NOTICE 或版本特定义务。本决定不是对未知、禁止或任意复杂许可证的通用豁免，不构成法律意见，也不免除保留许可证文本、版权、署名、NOTICE 或适用文件级源码义务。补充证据必须由独立 canonical 契约逐项保存精确 identity、证据类型、许可证结论、immutable source 或 tarball 文件区段、原始 source SHA-256、实际采用文本 SHA-256、限制和 `D-082`；owner exception 集合在代码中固定为上述 12 项，且每项继续绑定最终 lock 的官方 `resolved`、integrity、逐包 `evidenceSha256` 与 admissions，任一版本、来源、integrity、文本或证据摘要变化即自动失效。`@jsonjoy.com/json-pointer@17.67.0` 等同名其他版本不在例外内。首次策略只新增审核包列明的简单 SPDX 类别和精确 reviewed 表达，未列表达继续失败关闭；`require-like@0.1.2` 依据包内 `License` 结论为 MIT，含 OR 的列明表达选择审核包记录的 MIT 分支。三个带保守或实际 lifecycle 标记的包一律记录为 `ignored`，所有安装继续强制 `--ignore-scripts`，没有任何脚本执行例外。本授权不增加直接依赖、非官方 registry 包、浏览器第三方请求、第三方服务或用户数据处理，也不包含 commit、push、Action、服务器、DNS、凭证或生产操作。
- **D-083 / 2026-07-21**：用户在 #21 达到仓库实现与真实依赖图本地验收闭环后，授权把当前 `dev` 工作区中 D-080 本地 Node/hook 收口与 #21 的文档、manifest/lock、供应链实现、契约、正式制品和测试作为一个完整提交，按仓库提交格式 commit 并 push 到 `origin/dev`；推送后必须锁定该提交 SHA 触发的现有 GitHub Actions runs，持续观察到所有相关 run 均为 `completed` 且 `conclusion=success` 后才交回用户 review。CI 失败时只可在当前提交范围内定位、本地复现、修复、重跑并追加提交后再次观察；若修复需要改变已确认产品/依赖/安全边界、目标 Action 拓扑或扩大外部操作，必须重新请求确认。本授权不包含切换或改写分支历史、force push、向 `main` 推送、创建或合并 PR、关闭或修改 GitHub Issue、修改 Action 接线、操作凭证、服务器、DNS、云资源或生产环境；现有 CI 成功也不表示这些后续能力已经实现。
- **D-084 / 2026-07-22**：用户授权 Agent 从 #21 起按已确认的 Roadmap 依赖图连续收口执行 Issue；只有对应子 Issue 的主要不变量、正常路径、至少一个能使错误实现变红的反例、相关回归和该项要求的远端证据全部通过后，才可向该 GitHub Issue 发布不含凭证、隐私、受限原始报告或其他敏感内容的验收与下游交接摘要，并以 `completed` 原因关闭。每次关闭后必须先形成可恢复现场的紧凑摘要，再按 `blocked-by` 执行语义压缩和定向重载；父 Issue 仍须等全部子项及最短真实集成链闭合后才能关闭。某项验收失败、证据缺失、设计发生分歧或需要扩大权限时必须保持 Issue 开放并暂停下游，不得用历史绿灯、全量绿或建议方案替代定向证据。本委托只覆盖已拆解 Roadmap Issue 的脱敏验收评论与关闭，不授权创建或改写 Issue 范围、commit、push、PR、merge、分支或历史操作，也不授权 Action/凭证接线、服务器、TAT、DNS、证书、云资源或生产操作；这些操作继续逐项执行原门禁。
- **D-085 / 2026-07-22**：用户为 #22 的真实类型检查与最小生产构建验收，授权 Agent 在本任务专用临时目录中按已经由 #21 准入的 `package-lock.json` 联系官方 `https://registry.npmjs.org/`，通过 E-010 隔离入口执行一次冻结安装，并运行仓库定义的 `typecheck` 与 production `build`。安装必须使用全新且不复用的 HOME、npm config 与 cache，强制 `--ignore-scripts`、关闭 audit，不得使用镜像、代理、自定义 CA、凭据或根工作区 `node_modules`；冻结安装与测试过程不得自行修改依赖声明、版本、manifest、lockfile 或 #21 供应链制品，#22 在 D-078 委托内形成的非依赖脚本与模块解释边界变更仍须作为工作区实现 diff 独立受审。该过程可能向 npm 官方服务发送锁文件中的包名、版本以及 Node/npm/平台/架构元数据，不发送站点内容、访问者、账户或评论数据。验收必须同时证明安装前后 manifest/lock 摘要不变、`typecheck` 成功、production `build` 成功及约定反例失败；临时安装与构建目录在证据提取后清理。本授权不包含 commit、push、PR、merge、Action 接线、GitHub Issue 范围变更、服务器、DNS、凭据、云资源或生产操作。
- **D-086 / 2026-07-22**：用户在 #22 完成本地不变量、正常路径、反例与全量回归后，授权 Agent 将当前 `dev` 工作区中的 I-04 实现、测试、文档及 D-084 至 D-086 决策记录作为一个完整提交，按仓库提交格式 commit 并 push 到 `origin/dev`；推送后必须锁定该提交 SHA 触发的现有 GitHub Actions runs，持续跟踪到所有相关 run 均为 `completed` 且 `conclusion=success`。若 CI 失败，只可在 #22 已确认范围内定位、修复、重跑、追加提交并继续跟踪；需要改变依赖图、产品或安全边界、目标 Action 拓扑或扩大外部操作时必须重新请求确认。CI 成功后按 D-084 发布脱敏验收与 #11 交接摘要并以 `completed` 关闭 #22；#11 只能在关闭后完成语义压缩与定向重载再启动。本授权不包含切换或改写分支历史、force push、向 `main` 推送、创建或合并 PR、修改 Action 接线、操作凭证、服务器、DNS、证书、云资源或生产环境。
- **D-087 / 2026-07-22**：用户为 #11 的 E-012 真实验收授权 Agent 只在任务专用临时目录中操作：先复制当前候选工作树，通过 E-010 的全新 HOME、空 user/global 配置与私有 cache 联系官方 `https://registry.npmjs.org/`，按唯一 `package-lock.json` 执行冻结安装并强制 `--ignore-scripts`、关闭 audit；再只从官方 `https://nodejs.org/` 下载固定的 Linux x64 Node `24.16.0` 发行归档，严格核对仓库记录的 URL、文件大小上限、SHA-256、归档成员和提取后运行时身份。验收在同一临时候选上以主端点 Node `24.18.0` 和最低端点 Node `24.16.0` 执行相同 `npm test` 负载，并覆盖合法 `.js` 跨模块路径、非法无扩展名/`.ts`/alias/目录猜测、空测试集、编译/执行/清理失败、诊断保留、仓库零 emit，以及 `typecheck` 与 production `build` 回归；manifest、lockfile 与供应链制品前后摘要必须不变，所有任务临时目录在证据提取后清理。该过程可能向 npm 官方服务发送 lock 中的包名、版本及 Node/npm/平台/架构元数据，并向 Node.js 官方服务发送标准 HTTPS 请求元数据；不发送站点内容、访问者、账户或评论数据，不使用镜像、代理、自定义 CA、凭据、共享 cache 或安装脚本，不在仓库根创建 `node_modules`，也不修改依赖图。本授权不包含 commit、push、PR、merge、Action 接线、GitHub Issue 写操作、服务器、DNS、凭据、云资源或生产操作。
- **D-088 / 2026-07-22**：用户在 #11 的本地不变量、正常路径、约定反例、双 Node 端点和全量回归全部通过后，授权 Agent 将当前 `dev` 工作区中的 I-05 实现、测试、文档及本决定作为一个完整提交，按仓库提交格式 commit 并且只 push 到 `origin/dev`；推送后必须锁定该提交 SHA 触发的现有 GitHub Actions runs，持续跟踪到所有相关 run 均为 `completed` 且 `conclusion=success`。若 CI 失败，只可在 #11 已确认范围内定位、复现、修复、追加提交并继续跟踪；需要改变依赖图、产品或安全边界、Action 拓扑或扩大外部操作时必须重新请求确认。CI 成功后按 D-084 发布脱敏验收与 #23 交接摘要并以 `completed` 关闭 #11，随后先形成可恢复摘要并完成语义压缩，才能启动 #23。本授权不包含向 `main` 推送、创建或合并 PR、切换或改写分支历史、force push、修改 workflow 或凭证、操作服务器、TAT、DNS、证书、云资源或生产环境。
- **D-089 / 2026-07-22**：用户为按依赖链逐项验收 #23 明确授权在任务专用临时目录中执行真实验收测试。候选工作树只能复制到 `/tmp`，通过 E-010 的隔离边界使用唯一已准入 `package-lock.json` 联系官方 `https://registry.npmjs.org/` 做冻结安装，强制 `--ignore-scripts`、关闭 audit，并以仓库固定的 Node `24.18.0` 主端点和 `24.16.0` 最低端点运行同一 `npm test`、严格 TypeScript 与相关质量回归；所需 Node 归档只能来自 `https://nodejs.org/`，必须按既有供应链元数据校验 URL、大小、SHA-256、归档成员和运行时身份。该过程可能向两项官方服务发送标准 HTTPS 请求元数据及 lock 中的包名、版本和平台信息，不发送站点内容、访问者、账户或评论数据；不得使用镜像、代理、自定义 CA、凭据、共享 cache、安装脚本或根工作区 `node_modules`，不得修改 manifest、lockfile、依赖准入制品或依赖图，提取证据后必须清理临时目录。本授权不包含 Git/GitHub 写操作、Action 接线、服务器、DNS、证书、云资源或生产操作。
- **D-090 / 2026-07-22**：用户授权在 #23 的本地逐条验收和完整回归通过后，把当前 `dev` 工作区中仅属于 I-06 的实现、测试、设计修正及 D-089/D-090 记录形成一个提交并且只 push 到 `origin/dev`；推送后必须锁定该提交 SHA 触发的现有 GitHub Actions runs，持续跟踪到全部相关 run 均 `completed` 且 `conclusion=success`。CI 失败时只可在 #23 已确认范围内定位、修复、追加提交并继续跟踪；若需要改变依赖图、产品/安全边界、workflow 或扩大外部操作，必须重新请求确认。远端成功后按 D-084 回填脱敏验收、关闭 #23，并在开启下游 Issue 前形成可恢复摘要和语义压缩。本授权不包含向 `main` 推送、创建或合并 PR、切换或改写分支历史、force push、修改 workflow/凭证，或操作服务器、TAT、DNS、证书、云资源和生产环境。
- **D-091 / 2026-07-22**：用户指定 M0 开发界面的交接点为 #26 完成逐条验收、精确提交 CI 成功、Issue 关闭且语义压缩完成之后，并要求 Agent 在启动 #27 前明确提醒切换到 Codex Desktop。依赖链据此保持 `#23 -> (#5 + #6 -> #7) -> #26 -> 开发界面交接 -> #27`；#26 仍属于单一 docs 实例、扫描、内容投影与构建基础设施，#27 才开始页面和公开表达实现。本决定只记录提醒时点，不授权提前启动下游、改变技术/产品边界、安装桌面软件或执行任何额外 Git/GitHub/基础设施操作。
- **D-092 / 2026-07-22**：用户授权 Agent 继续按已拆解 Issue 的 `blocked-by` 顺序逐项完成，每项启动前先形成可恢复摘要并做语义压缩，逐条执行正常路径、反例、相关回归和任务专用临时目录验收；本地闭环后每个 Issue 形成一个聚焦提交，只 push 到 `origin/dev`，锁定精确 SHA 跟踪现有 CI 至 `completed/success`，再按 D-084 回填脱敏证据并关闭对应 Issue。失败修复只能留在当前 Issue 已确认范围内；本委托不允许同时堆叠多个未验收 Issue，也不授权改变产品、公开事实、依赖图、数据或安全边界，不包含向 `main` 推送、创建或合并 PR、切换或改写分支历史、force push、修改 workflow/凭证，或操作服务器、TAT、DNS、证书、云资源和生产环境。#5 是本委托在当前依赖链上的首个适用项；#26 闭环后仍须按 D-091 暂停并提醒界面交接。
- **D-093 / 2026-07-23**：#26 的真实公开项目 fixture 证明 E-007 强制登记的 `previewImage` 会进入 E-008 production 白名单，而 E-008 又要求每个公开主预览被 SSR HTML 的真实 `img[src|srcset]` 引用；Docusaurus 默认项目 docs 详情不消费该投影，因此只要出现合法公开项目，严格制品检查就会失败。用户选择方案 A，授权 #26 在单一已验证内容投影内增加最小、无样式的 SSR 主预览渲染，只能从对应项目已经验证的 `previewImage` 派生 `src`、`alt` 和固有尺寸，不读取第二份注册表、不接受正文手写公开路径，也不得用隐藏节点、metadata 或 checker 豁免伪造引用。production 只向 `published|archived` 详情注入；E-009 preview 可对已登记预览且确有已验证正文的 `draft|planned` 项目使用同一投影，但没有正文的未发布项目只保留可审查素材，不伪造详情或路由。该授权只闭合 #7 与 #26 的构建交叉层，不决定图片布局、文案、响应式、装饰或完整项目详情表现；#27 仍拥有页面与公开表达，并须消费同一安全投影替换或完善表现而不重复渲染。验收必须以包含 published/archived 项目、公开与 draft 文章的全新真实 Docusaurus fixture 证明唯一详情路由、侧栏、canonical、sitemap、私有日期索引、真实图片引用和未发布隔离，并以缺失、错误归属或第二来源反例失败关闭；未通过前不得提交、推送或关闭 #26。
- **D-094 / 2026-07-25**：用户授权 Agent 单独收口 #55。真实验收只在 `/tmp` 的本任务专用工作树副本中进行，通过 E-010 隔离入口使用唯一已准入 `package-lock.json` 联系官方 `https://registry.npmjs.org/` 执行冻结安装，使用全新且不复用的 HOME、npm config 与 cache，强制 `--ignore-scripts`、关闭 audit，并以 Node `24.18.0` 运行仓库定义的 `test`、`typecheck`、`quality` 和 production `build`；验收须证明 manifest/lock 前后字节不变，完成后清理任务副本。该过程可能向官方 npm 服务发送锁文件中的包名、版本及 Node/npm/平台/架构元数据，不发送站点内容、访问者、账户或评论数据。全部本地门禁通过后，Agent 可把 #55 范围内的实现、定向 fixture、精确模块测试入口和本授权记录形成一个聚焦提交，只 push 到 `origin/dev`，锁定精确 SHA 跟踪现有 CI 至 `completed/success`，随后向 #55 回填脱敏验收证据并以 `completed` 原因关闭。CI 失败时只能在 #55 已确认范围内修复、追加提交并继续跟踪；本授权不允许修改依赖图、公开事实、数据或安全边界，不包含创建或合并 PR、向 `main` 推送、切换或改写分支历史、force push、修改 workflow/凭证，或操作服务器、TAT、DNS、证书、云资源和生产环境。
- **D-095 / 2026-07-25**：用户在 #55 已按 D-094 完成独立验收、精确 `dev` 提交 CI 成功并关闭后，进一步授权把当前 `dev` 中仅属于 #55 的已验收提交与本授权记录通过仓库固定晋级路径合入 `main`。Agent 可将本记录作为独立文档提交 push 到 `origin/dev`，锁定其精确 SHA 等待 `dev` CI 达到 `completed/success`，随后创建唯一 `dev -> main` PR；PR 必须明确 #55 范围、验证证据、无依赖及外部数据边界，且在 required checks 全部成功、head/base 未漂移并确认可合并后，使用不改写 `dev` 历史且不删除长期分支的普通 merge 合并。合并后须锁定远端 `main` 的精确 merge SHA，等待对应 CI 达到 `completed/success`，再按仓库规范只以 fast-forward 同步本地 `main` 并切回 `dev`。若任一 CI 或合并门禁失败，只能在 #55 或本授权记录范围内查明原因；扩大代码、依赖、workflow、产品、数据或安全边界须重新确认。本授权不允许 force push、rebase、删除分支、绕过检查、直接 push `main`、修改 workflow/凭证，或操作服务器、TAT、DNS、证书、云资源、production artifact 和生产环境。
- **D-096 / 2026-07-26**：用户仅授权为 GitHub 仓库 `lyty1997/AxialMuseWebsite` 开启 Dependency Graph 与 Dependabot Alerts，用 GitHub 对默认分支 manifest/lockfile 的只读分析发现已知漏洞。该分析在现有 GitHub 托管边界内处理依赖名称、版本和依赖关系，不接收站点访问者、账户、评论或浏览器请求数据。明确不创建 `.github/dependabot.yml`，不开启 Dependabot version updates、Dependabot security updates、自动修复 PR、自动合并、自动分派 Agent 或 auto-triage 规则，也不修改依赖图、workflow、分支或仓库内容。实施只允许调用 GitHub 官方漏洞告警设置；验证必须证明 vulnerability alerts 已启用、dependency graph 随之启用、automated security fixes 仍未启用且仓库不存在 Dependabot 配置文件。本授权不包含 commit、push、PR、merge、Issue 写操作、其他仓库设置、凭证范围扩张或基础设施操作。当天已通过 GitHub 官方 vulnerability alerts 接口实施：终态检查返回启用状态，Dependency Graph 可提供该仓库的 SPDX SBOM，automated security fixes 保持 `enabled: false`、`paused: false`，默认分支 `main` 的 `.github` 目录不存在 `dependabot.yml`；本次没有生成更新 PR、改动依赖图或修改 workflow。
- **D-097 / 2026-07-26**：用户在当前 CI/CD 审计后确认按“可信 CI → `main` required checks 与合并门禁 → immutable production artifact 与 GitHub `production` environment/审批 → TAT/Nginx 部署、回滚、公网冒烟与定时检查”的顺序推进。本轮先实施第一阶段，授权修改仓库内 CI 设计状态、workflow、零依赖质量脚本和 fixture：建立失败关闭的 `website-quality`、`node-minimum`、`diagrams` 与 `supply-chain` 四个 job；主端点固定为 `.nvmrc` 的 Node `24.18.0`/npm `11.16.0`，最低端点固定为 `engines.node` 下界 Node `24.16.0`/npm `11.13.0`，两个构建 job 使用完整非浅 checkout、关闭 checkout 凭证持久化、通过 E-010 全新隔离 cache 冻结安装并分别执行 `quality`、`typecheck`、`test` 与 production `build`；同时补齐 E-013 HEAD 可达历史门禁和 workflow 拓扑正反 fixture，供应链 job 对含开发依赖的锁定图执行受限 live audit，`moderate`、`high`、`critical` 阻断且 registry 不可用时失败关闭。现有三个 GitHub 官方 Action 经官方 release 与 commit API 核验后准入并固定为 `actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09`（`v5.1.0`）、`actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444`（`v5.0.0`）和 `actions/setup-java@03ad4de0992f5dab5e18fcb136590ce7c4a0ac95`（`v5.6.0`）；三项 commit 的 GitHub verification 均为 `verified=true`、`reason=valid`，不得改回浮动 tag，不启用 setup-node/package-manager cache，也不新增其他 Action。冻结安装和 live audit 只联系官方 npm registry，可能发送依赖名称、版本、完整 lockfile 树及 Node/npm/runner 平台元数据，不发送站点内容、访问者、账户或评论数据，不产生浏览器请求。第一阶段的 `GITHUB_TOKEN` 仅有 `contents: read`，不使用 write、OIDC、attestation、repository/environment Secret，不上传 build 或 audit artifact，不创建部署身份，也不操作 GitHub environment、ruleset/branch protection、服务器、TAT、DNS 或证书。本授权允许完成本地实现与验证，但不包含 commit、push、PR、merge 或分支变更；第一阶段通过后，第二阶段须先报告私有仓库当前套餐的可强制能力与互斥门禁选项，再取得远端设置和 Git 发布的明确授权，第三、四阶段继续分别受 artifact/审批/凭证及生产基础设施门禁约束。

- **D-098 / 2026-07-26**：D-097 双端点 production build 发现，E-008 随机构建上下文的绝对 `staticDirectory` 被 Docusaurus 3.10.2 随 `siteConfig` 序列化进公开 JavaScript，造成构建机路径泄漏及同输入因临时根不同而产生公开 chunk/HTML 漂移。用户选择方案 1，对 D-097 作窄范围扩展，并只替代 E-008 的静态白名单传输方式：继续保留随机、私有、同用户、权限 `0700`、owner 绑定的系统临时白名单树，以及既有扫描、快照、物化、身份复核和失败清理模型；根配置的 `staticDirectories` 固定为空数组，不再把仓库根、构建根、事务根、候选输出或系统临时绝对路径写入可序列化 `siteConfig`、plugin options、global data 或浏览器模块。仓库内仅服务端的本地 Docusaurus 插件使用固定 Docusaurus 3.10.2 支持的公开 `postBuild({outDir})` 生命周期，在断链检查前把已经物化并验证的白名单树逐路径、逐字节复制到本次候选 `outDir`；绝对源路径只存在于服务端闭包，目标冲突、缺失、多余、链接、逃逸、身份漂移或复制失败均失败关闭。`postBuild` 只负责复制及既有私有摘要写入，不承担最终成功判定；外层 fresh production checker 仍是唯一最终验收者。

  本轮授权可修改本决定及相关设计状态、`docusaurus.config.ts`、#26 的 production 构建适配、本地插件、受控制品检查器、必要的构建入口、测试/fixture 与质量接线。新增制品门禁必须拒绝仓库根、候选输出、generated files、受控构建/事务临时根等服务端路径进入 HTML、JavaScript、JSON、XML、CSS 等文本制品；不得靠构建后字符串替换、重写 bundle 或忽略 checker 消除证据。回归必须使用至少两个不同的随机私有临时根，分别证明白名单素材完整且各自路径不泄漏，并以路径注入反例证明门禁会变红；该验证只证明路径独立与泄漏关闭，不把 E-015 提升为跨 runner、跨 Node 或完整制品树逐字节一致保证。本授权不改变 production/preview 白名单成员、公开 URL、状态过滤、未发布素材隔离、内容投影、UI 或公开文案；不允许回退到仓库 `static/`、`public/` 或 `site-assets/`，不实现 #8 preview 候选激活，不导入 Docusaurus 私有 API，不新增、升级或删除依赖，也不修改 manifest、lockfile 或供应链制品。

  用户同时明确授权在任务专用 `/tmp` 副本中，以 Node `24.18.0`/npm `11.16.0` 和 Node `24.16.0`/npm `11.13.0` 分别通过 E-010 使用全新私有 HOME/config/cache，仅连接官方 `https://registry.npmjs.org/` 执行冻结安装，并运行完整 `quality`、`typecheck`、`test`、production `build` 和包含开发依赖的受限 live audit；该联网可能发送依赖名称、版本、完整 lockfile 树及 Node/npm/平台元数据，不发送站点内容、访问者、账户或评论数据，不执行安装脚本、不使用镜像、共享 cache 或仓库根 `node_modules`。验收须证明 manifest/lock 前后字节不变、live audit 阈值通过且仓库根不产生 `node_modules`。本窄扩不授权 commit、push、PR、merge、分支或 GitHub 设置变更，也不授权 Action/artifact、Secret、production environment、required checks、服务器、TAT、DNS、TLS 或其他生产操作。

- **D-099 / 2026-07-26**：用户明确要求普通 CI 不再被 live audit 阻断，并允许将其剔除。本站选择从普通 `push`/`pull_request` workflow 直接移除 live npm audit，而不引入 `continue-on-error`、`always()`、`|| true` 或修改审计脚本退出码的半门禁；这样四个 prerequisite job 继续保持无失败绕过的明确结论，`supply-chain` 只对当前 lockfile 的静态来源、integrity、许可证、生命周期脚本、admissions、SBOM/NOTICE 和 manifest/lock 漂移失败关闭。该决定只替代 D-097 中“普通 CI 的 `supply-chain` job 每次联网 audit 并据此阻断”以及 D-098 中“live audit 阈值通过才完成本轮普通 CI 验收”的条款，不替代 D-077/E-010 的显式依赖首次准入或依赖图变化后的重准入协议；这些受审流程仍必须通过隔离 `audit` profile 检查含开发依赖的完整图，继续执行 `moderate`、`high`、`critical` 阻断、`low` 报告、服务不可用时失败关闭并禁止 `npm audit fix`。

  D-096 已启用的 Dependency Graph 与 Dependabot Alerts 继续承担默认分支的持续漏洞发现，人工依赖维护可按需显式运行受限 audit；普通 CI 不再调用 `scripts/quality/run-supply-chain-audit.mjs`，audit 结果和 registry 可用性都不参与 job 或 workflow conclusion。2026-07-26 已观测到的 18 个 high 依赖节点及其 `brace-expansion` advisory 仍是未修复的已知风险，不因移出普通 CI 而视为已修复、已接受或可忽略。本决定不修改 `package.json`、`package-lock.json`、admissions、SBOM/NOTICE、依赖策略或漏洞阈值，不执行依赖更新、联网审计、Git 发布、GitHub 设置或生产基础设施操作；未来若增加独立 scheduled audit 或把 live audit 恢复为门禁，须重新确认其结论语义和外部请求边界。
- **D-100 / 2026-07-27**：用户授权以本地精确提交 `9df4ba5678fc251d4882df5d5867e6d4990789e7` 为基点创建并切换到 `codex/ci-issues-12-32`，保留当前工作区已有改动，在该分支完成并本地提交 D-097 至 D-099 的可信 CI 第一阶段、#12 的 E-013/CODE-018 完整 Git 历史身份门禁，以及 #32 的四个固定 Action prerequisite jobs 与静态 workflow 契约。D-098 为使同一双端点 production build 不泄漏私有绝对路径而形成的窄范围构建修复属于本提交闭环；不得借此改变公开 URL、内容可见性、依赖图、浏览器请求、用户数据或生产边界。用户指定的精确提交是本分支唯一集成基线；本分支不宣称包含该基点之后的任何远端提交或 #27/#28 实现，未来若需整合须在保持双方已验收语义的前提下另行授权。提交前必须复核工作区范围，并在 Node `24.18.0` 主端点与 Node `24.16.0` 最低端点按既定 E-010 隔离边界验证相关 `quality`、`typecheck`、`test` 与 production `build`；至少保留 #12 的正常/反例 DAG、#32 的 workflow mutation、D-098 的路径泄漏反例和 `git diff --check` 证据。本授权不允许 fetch、pull、rebase、merge、改写或删除分支，也不包含 push、PR、Issue 评论/关闭、GitHub Actions 实跑、required checks/ruleset、artifact、environment、凭证、服务器、TAT、DNS、TLS 或其他生产操作；这些远端和基础设施动作仍须分别授权。
- **D-101 / 2026-07-27**：用户在 D-100 的本地提交完成后，单独授权把当前 `codex/ci-issues-12-32` 以普通非强制 push 推送到 `origin` 的同名临时分支，并为本地分支设置该远端 ref 为 upstream。推送前必须保持工作区干净、确认 CI/#12/#32 实现提交 `11a41aae24baad9ddd4a4c9be68ff8ce02e063d0` 的唯一父节点仍为 D-100 精确基点 `9df4ba5678fc251d4882df5d5867e6d4990789e7`，并把本授权记录作为该实现提交的纯文档后继纳入待推送 tip；推送后以 Git 返回成功、本地 upstream 关系及 `git ls-remote --heads origin refs/heads/codex/ci-issues-12-32` 精确等于本地 `HEAD` 共同验收。现有 workflow 的 `push` 触发器只包含 `main` 与 `dev`，因此同名临时 ref 交付本身不产生该提交的 GitHub Actions run，也不等于 #12/#32 远端验收、required checks 或合并闭环。本授权仅窄幅替代 D-100 的 push 禁止，不授权 fetch、pull、rebase、merge、force push、删除或改写分支，不触碰 `main`/`dev`，也不包含 PR、Issue 写操作、GitHub 设置、Actions 重跑、artifact、environment、凭证、服务器、TAT、DNS、TLS 或其他生产操作。
- **D-102 / 2026-07-27**：#12 的实现评审确认，D-097 把依赖冻结的 E-013 历史 CLI 及其 fixture 直接加入 `quality` 后，干净克隆在没有 `node_modules/` 时会由 `@docusaurus/utils@3.10.2` 缺失而阻断既有零第三方依赖质量入口与 pre-commit；CI 因先执行 E-010 冻结安装而未暴露该回归。本站保留 CODE-004/E-013 的同一 Docusaurus 公共结构化解析器，不另写 YAML、逐行或正则解析器，也不让 hook 自动联网安装依赖；改为从 `quality` 聚合中移出历史 CLI/fixture，并以独立 `scripts/quality/run-content-history.mjs` 在 E-010 冻结安装后依次执行真实工作区门禁、完整 DAG fixture 与冻结解析器集成 fixture。`website-quality` 与 `node-minimum` 都必须在 `quality` 后、`typecheck`/`test`/`build` 前运行该独立入口，继续保持完整 checkout、失败关闭、双端点、38 个历史 fixture 和 2 个解析器集成 fixture 覆盖；本地 pre-commit 只承担原有零依赖 `quality`，需要本地运行 E-013 时由作者先显式完成既定冻结安装再调用独立入口。历史提交与当前工作区缺少冻结解析器时均以稳定 `CONTENT_HISTORY_DEPENDENCY` 失败，runner 对子进程启动错误、信号和任一非零退出均首错即停。该决定只修正 D-097/D-100 的门禁编排以及 E-015 中“历史属于 quality 内部”的表述，不降低 #12 的解析、历史或 CI 验收语义，不改变依赖图、公开内容、浏览器请求、用户数据或生产边界。用户明确授权完成本修复后形成聚焦提交，并普通非强制 push 到当前 `codex/ci-issues-12-32` 同名远端临时分支；不授权 PR、merge、Issue 写操作、`main`/`dev` 变更或其他远端与生产操作。
- **D-103 / 2026-07-28**：用户明确授权把当前 #24 的直接 Node 作者入口、UUIDv7 模板、E-013 候选与终态历史检查、原子目录事务、作者/build 双锁、消费者残留门禁、显式作者验收 runner、测试和同步文档作为一个聚焦提交，提交到现有 `codex/ci-issues-12-32`，随后普通非强制 push 到 `origin` 同名临时 ref。提交前必须证明 8 个新增源码/测试文件与 17 个消费者、门禁、测试和文档修改处于同一完整补丁，并在从当前 `HEAD` 仅应用该补丁的 fresh checkout 中通过无 `node_modules/` 的 E-010 `run-script quality`、使用同一 lockfile 冻结依赖的作者验收与 production build，以及 `git diff --check`；push 后以 Git 返回成功、本地 upstream 和远端同名 ref 精确等于本地 `HEAD` 验收。该临时 ref 不在现有 `main`/`dev` push 触发范围内，因此本授权不构成远端 CI 或 Issue 验收，也不授权 fetch、pull、rebase、merge、force push、PR、Issue 写操作、`main`/`dev` 变更、GitHub 设置、凭证、服务器或其他生产操作。
- **D-104 / 2026-07-28**：用户确认 #27 与 #28 已合入 `main`，并授权将 `codex/ci-issues-12-32` 中已经提交的可信 CI、#12、#24 与 #32 变更按仓库固定晋级路径整合到 `main`。实施以刷新后的 `origin/dev@c3c9f6f`、`origin/main@d00000e` 和专题分支已推送基点 `491d04a` 为起点：先把本授权记录普通非强制 push 到专题分支，再在本地 `dev` 以普通 merge 纳入专题分支，解决冲突时必须同时保留 #27/#28 已验收的页面、主题、导航、测试和完成证据，以及专题分支的完整历史身份门禁、四 job workflow、零依赖 pre-commit、作者原子事务与残留检查；不得用整侧覆盖、降低断言或删除一方证据规避冲突。完成本地完整质量、历史、类型、测试与 production build 验证后，普通非强制 push `origin/dev`，锁定精确 SHA 观察其 CI 至全部相关 run `completed/success`；随后创建唯一 `dev -> main` PR，在 required checks 成功、head/base 未漂移且 GitHub 仍报告可合并后使用普通 merge，不删除长期分支，并锁定合并后的精确 `main` SHA 观察对应 CI 至 `completed/success`。最后只以 fast-forward 同步本地 `main` 并切回 `dev`。若冲突解决需要改变已确认产品表达、依赖图、workflow job 边界、历史/事务不变量或扩大安全与外部操作范围，必须暂停并重新确认。本授权不允许 rebase、force push、直接 push `main`、绕过检查、删除或改写分支、修改 GitHub 设置或凭证、执行 Issue 写操作，或触碰 artifact、production environment、服务器、TAT、DNS、TLS 及其他生产基础设施。

## 依 D-078 形成的 M0 工程决定

- **E-001 / 2026-07-18 — 项目内容职责拆分**：`docs/contracts/projects.json` 继续拥有项目 ID、短 slug、标题、摘要、状态、日期、仓库、展示开关、导航顺序和写作模块等结构化事实；项目长文位于 `site-content/projects/<project-id>/index.md` 或 `index.mdx`，只拥有背景、能力、取舍、限制、证据说明和复盘正文，不重复 H1、摘要、状态、日期、仓库或路由字段。构建期根据稳定 project ID 关联两者，并从注册表只读派生 Docusaurus 的 `title`、`description`、完整 `/projects/<project-slug>` 路径和草稿行为，不回写源文件。`published` 或 `archived` 项目必须恰有一个正文入口；`draft` 或 `planned` 可以暂缺正文但不能产生生产路由；孤儿正文、双入口和结构化字段双写均使构建失败。项目列表、项目侧栏和详情元数据读取同一注册表，正文文件路径不生成公开 URL。
- **E-002 / 2026-07-18 — URL 与路由闭包**：站点显式使用 `trailingSlash: true`；文章源 `slug` 和项目注册表短 slug 均不保存末尾 `/`，Docusaurus 构建链接与 canonical 统一输出末尾 `/`，Nginx 将无末尾斜杠的页面请求永久重定向到规范地址。Docusaurus `onDuplicateRoutes`、断链和锚点检查使用失败关闭；构建门禁在规范化后统一比较 `src/pages`、docs、静态资源和重定向。首版内容路由只允许 `/`、`/projects/`、`/projects/<project-slug>/`、`/writing/` 与已发布 `/writing/<article-slug>/`；`/assets/`、`/img/`、`/.well-known/`、`/404.html`、`/robots.txt` 和 `/sitemap.xml` 为框架或运维保留空间。重定向登记只接受同站精确旧路径到精确新路径，禁止通配、链、环、自重定向和与活动路由冲突；系列、主题、作者、归档、筛选、分页、RSS 与独立 `/about/` 路由不进入 M0。Docusaurus 对 [`trailingSlash` 和 `onDuplicateRoutes` 的官方配置语义](https://docusaurus.io/docs/api/docusaurus-config)是本决定的框架依据。
- **E-003 / 2026-07-18 — 内容注册表与发现关系**：作者注册表使用 `docs/contracts/authors.json` 的对象型 ID 映射，GitHub 链接对新增作者可选；受控主题使用 `docs/contracts/topics.json` 的对象型 ID 映射；项目写作模块嵌套在对应 `projects.json` 项目的 `writingModules` 中，并由 `(project ID, module ID)` 唯一定位。无项目文章进入只存在于导航投影中的“通用技术”分组，不伪造项目 ID。跨项目关系使用可选 `relations.projects` 稳定项目 ID 列表，只提供相关入口，不改变规范侧栏归属；主题首版只用于元数据显示和校验，不生成主题路由。所有注册表 ID 使用 lowercase kebab-case，显示名与排序可变但 ID 不复用；未知字段、悬空引用、重复项和同级排序冲突失败关闭。
- **E-004 / 2026-07-18 — Docusaurus 页面与主题适配**：首页、`/projects/` 和 `/writing/` 使用 `src/pages/` 的严格 TypeScript 页面；项目和文章详情由单一 docs 实例提供。首版保留 classic/Infima 的语义、导航与可访问性能力，通过 `themeConfig`、`src/css/custom.css` 设计令牌和页面级 CSS Modules 完成品牌化，不引入 UI 库，不使用 eject，关闭颜色模式切换并只验收亮色。默认不 Swizzle；只有真实浏览器 fit-gap 证明 CSS 和官方扩展点无法满足既定三栏职责时，才在精确锁定版本上包装最小主题组件并增加契约测试。`>=1280px` 显示完整三栏，`996-1279px` 折叠左栏并保留右侧目录，`<996px` 将左右目录都移到正文上方的可访问折叠区。M0 不增加搜索、筛选、分页、客户端滚动高亮或文章专属交互。
- **E-005 / 2026-07-18 — 静态构建与制品交付（运行配置安装边界由 E-014、job 字节所有权由 E-015 补充）**：Docusaurus 始终使用默认 `build/` 输出目录；PR 与 `main` 可以在 GitHub Actions 生成仅供门禁的 job-local build，但只有 canonical `main` 精确 `GITHUB_SHA` 的 `production-artifact` 可以生成可部署 build。生产服务器不安装 Node/npm、不拉取源码、不执行仓库脚本。通过门禁后，按 CODE-015 把 `build/` 复制为 artifact 的 `payload/`，并附精确提交标识和逐文件 SHA-256 metadata；E-014 后续要求 metadata 还包含从同一 payload 派生并绑定的两个可部署重定向文件，E-015 进一步要求这些字节由 `production-artifact` 在同一 job 内重新构建、复验和封装。固定 TAT command 只接收 workflow run/artifact 标识、提交 SHA、GitHub 外层 `artifactDigest` 与上传前独立计算的 `releaseContentSha256`，两个摘要不得合并成一个模糊参数。服务器从固定仓库读取 artifact 元数据，验证其 `head_sha`、两个期望摘要、归档路径安全和内部文件清单后，把已验证 payload 与 E-014 的可部署配置安装到同一 `releases/<sha>`；只有 payload 进入 Web Root，其他 metadata 留在 staging，本机冒烟通过才整版激活。公开仓库使用无需凭证的读取路径；私有仓库仅使用限于单仓库 `Actions: read` 的细粒度凭证，仓库可见性与方案能力在 OD-009 现场核验后选择对应分支。GitHub 官方说明 [workflow artifact 可在 job 间传递且上传返回 SHA-256 digest](https://docs.github.com/en/actions/tutorials/store-and-share-data)，[REST 下载端点返回短时重定向且细粒度令牌需要 `Actions: read`](https://docs.github.com/en/rest/actions/artifacts?apiVersion=2022-11-28)；具体 Action 版本与 commit SHA、凭证创建、TAT/服务器配置仍须按原门禁准入和操作授权。
- **E-006 / 2026-07-18 — 项目叙事所有权归一**：E-001 的正文所有权优先于迁移期字段表和注册表数据。`problem`、`decisions` 与叙事性 `evidence` 不属于 `projects.json`，也不形成项目正文 frontmatter；项目正文唯一拥有问题背景、能力与架构、关键取舍、限制、证据说明和复盘，正文证据链接不回填注册表。注册表继续唯一拥有列表、路由、状态和展示所需的结构化事实，其中 `summary` 是目录与元数据的一句话摘要，`source` 只追溯结构化事实，不充当页面证据清单。项目正文通过 `site-content/projects/<project-id>/` 目录名与注册表稳定 ID 绑定，不再写第二个 project ID；正文不含作者可编辑 frontmatter 或 H1，Docusaurus 的标题、描述、slug 和草稿行为只在构建内存中派生。自动门禁只拒绝可解析的项目 frontmatter、H1、注册表未知字段、孤儿正文、未知目录和双入口；正文可正常叙述问题、取舍和证据，摘要与正文是否存在不必要重复由内容审查判断，不以字符串相似度推断语义。

  在目标正文尚不存在的迁移期，DocRestore 的“主站叙事迁移源（过渡）”和 VibeCoding Project Scaffold 的“主站内容契约”章节分别是临时叙事所有者；它们必须先补齐旧注册表中仍有价值的事实。创建目标正文的同一提交须人工移动并审核叙事，再把过渡章节替换为正文的仓库相对链接，确保迁移前后始终只有一个可编辑来源。注册表中的迁移残留字段直接删除且不得作为生成来源。#5 已按该规则创建两份正文并把原过渡叙事替换为链接；`projects.json` 的结构化事实、发布状态和素材字段未改变，真实扫描、框架投影和构建装配仍由 #26 接管。该决定不批准尚未确认的素材、依赖或生产发布。
- **E-007 / 2026-07-18 — 项目主预览媒体契约**：`projects.json` 使用可选单对象 `previewImage` 明确选择项目列表和分享上下文中的唯一主预览，不从项目 ID、文件名或目录猜测。对象只包含 `sourcePath`、`width`、`height` 和 `alt`：`sourcePath` 是相对 `site-assets/` 的 POSIX 路径，固定为 `projects/<project-id>/<lowercase-kebab-case>.webp`；公开 URL 唯一派生为 `/assets/<sourcePath>`，不另存 `publicPath`。M0 只接受非动画 WebP、精确 1600 x 1000、最多 300,000 bytes，登记宽高必须与解码结果一致并作为渲染固有尺寸。`alt` 是去除首尾空白后的非空单行纯文本，最多 160 个 Unicode grapheme，不得与项目标题或摘要完全相同；它须说明图片所证明的真实界面或工程状态，语义充分性由内容审核负责。`published`、`archived` 项目必须恰有一个 `previewImage`，`draft`、`planned` 可有零或一个且不得用占位图。当前两个 `planned` 项目没有已确认素材，因此不伪造字段或文件；项目转为公开状态的同一变更必须完成人工公开性、隐私、凭证、版权和视觉证据审核。该决定解决 #6 的 schema 缺口，不构成对任何具体公开素材的确认。
- **E-008 / 2026-07-18 — 发布态静态素材白名单（传输方式由 D-098 窄幅替代）**：项目主预览原件只进入 `site-assets/projects/<project-id>/`，该目录永不直接配置为 Docusaurus `staticDirectories`；始终公开的品牌和根级静态文件进入 `static-public/`，其中禁止项目、文章或待审核素材。所有生产与审查构建都必须通过仓库内受控构建入口，在全新系统临时目录建立静态白名单树：`production` 物化 `static-public/` 和 `published`/`archived` 项目登记的预览，`preview` 物化 `static-public/` 和全部状态中已登记的项目预览；源 `site-assets/<sourcePath>` 映射到临时树 `assets/<sourcePath>`，最终 URL 为 `/assets/<sourcePath>`。D-098 只替代原先“把临时树绝对路径作为唯一 `staticDirectories`”的传输条款：根配置现在固定 `staticDirectories: []`，服务端本地插件在受支持的 `postBuild` 生命周期把同一已验证临时树复制到当前候选，绝对路径不得进入客户端可序列化配置或最终文本制品；本条其余白名单成员、状态过滤、快照、权限、身份与失败清理语义不变。直接调用 Docusaurus build 而未提供由入口生成并校验的模式与临时目录时必须失败关闭；`static/`、`public/` 和整个 `site-assets/` 均不得成为目标静态目录。

  I-12 在 2026-07-23 将“始终公开”收口为显式逐文件声明：`docs/contracts/static-public-assets.json` 是 `static-public/` 的唯一登记，目录中的普通文件必须与登记路径一一对应；条目只允许 `brand` 和 `operational` 两种角色，公开 URL 由登记的相对源路径唯一派生，不保存第二个 URL。`brand` 只允许 `assets/brand/` 或根级 favicon，`operational` 只允许 `robots.txt` 或 `.well-known/`；`assets/projects/`、`assets/writing/`、HTML、sitemap 和其他框架输出空间始终拒绝。机器门禁还要拒绝登记素材与任一项目媒体或调用方提供的未发布正文素材同字节。登记与目录闭合证明每个进入白名单的通用静态字节都经过显式分类，但不声称能从任意全新字节的视觉语义自动判断其真实性、隐私、版权或是否被恶意错标；这些事项仍须在文件和登记进入 Git 前人工审核。该澄清不批准任何真实素材，当前登记和目录都为空。

  源门禁拒绝符号链接、特殊文件、路径逃逸、大小写冲突、重复目标、跨项目目录引用和孤儿项目预览。生产制品中的 `assets/projects/**` 必须与生产白名单逐路径、逐字节相等，每个公开预览必须被 SSR HTML 引用；未发布预览的公开路径不得出现在任何文本制品中，其源 SHA-256 也不得与 `build/` 内任一文件相同，以覆盖改名或哈希泄漏。#26 将同一闭包扩展到 `site-content/projects/<entry>/assets/` 与 `site-content/writing/<entry>/assets/` 的全部未发布正文素材，并检查原始字节及其标准 Base64 表示，覆盖 Docusaurus 3.10.2 对小型 Markdown 图片的内联转换；不得因为哈希改名、data URL 内联或项目正文素材尚无真实样本而漏过生产泄漏。预览隔离只控制生产交付，不是保密边界：任何素材进入 Git 前仍须通过凭证、隐私与版权检查。Docusaurus 官方说明 [`staticDirectories` 可使用绝对路径](https://docusaurus.io/docs/api/docusaurus-config#staticdirectories)，并且[静态目录中的内容会原样复制到构建输出](https://docusaurus.io/docs/static-assets)；因此发布状态过滤必须发生在交给框架之前。该决定解决 #7 的生产泄漏冲突，不批准提交具体素材，也不改变 E-005 的默认生产输出目录。
- **E-009 / 2026-07-18 — 可验收草稿预览制品**：局域网预览只在 Linux 预览 worktree 对远端分支精确提交运行 E-008 的受控入口，模式固定为 `preview`，并由锁定的 Docusaurus `3.10.2` 执行 `build --dev --out-dir <candidate>`；继续使用现有 `python3 -m http.server` 提供构建后的静态文件，不引入 Docusaurus 常驻开发服务器或另一套 Windows 构建链路。preview 配置显式使用 `noIndex: true` 并关闭 sitemap，production 显式使用 `noIndex: false` 并生成站点地图；两种模式的 `url` 都保持 `https://www.axialmuse.com`、`baseUrl` 都为 `/`，局域网主机、IP 和端口只属于访问配置，不得写入 canonical、HTML、XML 或 JavaScript。技术文章 `publicationStatus: draft` 仍只读投影为原生 `draft: true`，preview 的“草稿”侧栏组可访问，production 的路由、侧栏、日期索引和 sitemap 必须完全排除 draft。Docusaurus 官方说明 [docs draft 只在 development 可用](https://docusaurus.io/docs/api/plugins/%40docusaurus/plugin-content-docs)、[`build --dev` 生成 development-mode 静态输出](https://docusaurus.io/docs/cli)、[`noIndex` 为每页加入 `noindex, nofollow`](https://docusaurus.io/docs/api/docusaurus-config)；三者在精确 `3.10.2` 下的组合仍须由构建 fixture 实证，不能只靠文档推断。

  预览状态使用必填的仓库与 worktree 外绝对路径 `PREVIEW_STATE_DIR`，并要求候选、release 和 `current` 位于同一文件系统：`candidates/<sha>.<pid>/` 是本次构建区，验证后原子重命名为不可变 `releases/<sha>/`，`current` 通过临时 symlink 加原子 rename 指向活动 release；PID、请求分支、候选 SHA、活动 SHA、最近失败和日志分别放在非公开的 `run/` 与 `logs/`。Python 服务始终服务 `${PREVIEW_STATE_DIR}/current`，活动制品不再依赖 worktree 当前 checkout。`restart` 在排他锁内只接受存在的远端分支，fetch 后 detached checkout 精确 `origin/<branch>`，校验 `.nvmrc`、当前 lock 与预先独立准备的本地冻结依赖，再构建并检查全新候选；它不得执行 `npm install`、`npm ci`、`npm audit`、依赖解析或下载。fetch、checkout、Node、依赖、build 或制品检查在切换前失败时删除候选、保持旧 `current` 与服务器 PID；切换后本机 HTTP 冒烟失败时恢复旧 symlink，首次启动没有旧版本时停止新服务。服务已运行且切换成功时 PID 不变；`stop` 只停服务，不删除活动制品；`status` 分别报告请求分支、worktree HEAD、活动制品 SHA、模式、PID/URL 与最近失败，不能把 checkout 成功误报为预览更新。该决定解决 #8 的设计缺口；当前 `preview.sh` 和环境示例仍是服务 `public/` 的迁移前实现，须在后续同一实现变更中迁移并完成真实局域网验收。
- **E-010 / 2026-07-18 — npm 启动前隔离与官方 registry 边界**：所有候选解析、冻结安装、显式 audit、原生 SBOM 和受控 npm script 必须通过零第三方依赖的 `scripts/quality/run-isolated-npm.mjs`；入口只接受 `resolve-lock`、`ci`、`audit`、`sbom-native`、`run-script` 五个封闭 profile 及各自白名单参数，不提供任意 npm 参数透传。未来根 `.npmrc` 只允许 `registry=https://registry.npmjs.org/`、`replace-registry-host=never`、`strict-ssl=true`、`ignore-scripts=true`、`audit=false`、`fund=false`、`update-notifier=false`、`package-lock=true` 和 `lockfile-version=3`，不允许 cache、认证、代理、CA 或 scoped registry。入口在启动任何 npm 子进程前，先校验工作目录、精确 Node/npm 端点、`.npmrc` 允许键、manifest 只含 registry semver 来源，并创建权限 `0700` 的临时 HOME、空 user/global npmrc、全新 cache/log/tmp；子进程环境不继承任何大小写形式的 `npm_config_*`、`NODE_ENV`、代理变量、`NODE_EXTRA_CA_CERTS` 或 `SSL_CERT_*`，只显式写入当前临时路径和固定配置。npm CLI 使用从当前 `process.execPath` 所属 Node 发行版推导并验证的绝对路径，不经 PATH 查找或下载 fallback。

  联网前在同一隔离环境执行离线 `npm config list --json`，校验默认 registry 唯一指向官方 HTTPS、没有 scoped registry、认证、证书或代理键，cache/userconfig/globalconfig 精确位于本次临时目录，脚本、audit、SSL 与 registry host 策略符合根配置；输出不可解析、敏感键存在或值不符即停止。`resolve-lock` 只允许主端点运行，并在命令行加入 npm 11.16 支持的 Git/file/directory/remote 来源禁用参数；这些新参数不写入共享 `.npmrc`，因为最低端点 npm 11.13 不支持。生成后两个端点共用的零依赖扫描器都必须拒绝非官方 registry、非 integrity 条目和非 registry spec，再允许各自在全新 cache 运行 `npm ci --ignore-scripts --audit=false`，并核对 manifest/lock 前后摘要不变。`audit` 明确包含 dev 全图并只联系官方 registry；`sbom-native` 以 offline/package-lock-only 运行；tarball 审查由内置 HTTPS 从已经验证的官方 `resolved` 下载到独立临时区，拒绝跨 origin 重定向并核验 integrity，不复用 npm cache。setup-node 不恢复 npm cache，workflow、package scripts 和策略检查拒绝绕过隔离入口。当前本机 `registry=https://registry.npmmirror.com` 与共享 `~/.npm` 证明事后 lockfile 扫描不足，但本决定不修改用户配置，也不授权实际 registry/audit/tarball 请求。依据为 npm 官方的 [config 来源与优先级](https://docs.npmjs.com/cli/v11/using-npm/config/)、[npmrc 层级](https://docs.npmjs.com/cli/v11/configuring-npm/npmrc/)和[scope 可独立绑定 registry](https://docs.npmjs.com/cli/v11/using-npm/scope/)。该决定解决 #9 的设计缺口。
- **E-011 / 2026-07-18 — npm SPDX 2.3 确定性规范化**：原生 `npm sbom --package-lock-only --sbom-format=spdx --sbom-type=application --offline` 是包与关系语义的输入，不直接成为提交文件。生成器严格验证 SPDX 2.3、npm creator、包、关系、校验和与 lock/tarball evidence 一致，只允许对 npm 每次变化的 `creationInfo.created`、`documentNamespace`，稳定 creator 补记、对象键和 SPDX 中无序集合的顺序做规范化；不修改包、许可证、关系或其他语义。规范器版本固定为 `axial-muse-supply-chain-1.0.0`，先移除两个易变字段、加入该稳定 creator，仅对明确列出的 packages、relationships、documentDescribes、creators、checksums 和 externalRefs 使用固定复合键排序，未知数组形态失败而非猜测，再以递归 ASCII 对象键、2 空格、LF 和单个末尾换行形成 canonical JSON。该语义输入连同 normalizer version 计算 `semanticSha256`。

  `docs/generated/supply-chain/dependency-evidence.json` 的 `sbom` 记录只包含 `normalizerVersion`、`semanticSha256`、`createdAt` 与最终 `fileSha256`。首次出现或 `semanticSha256` 变化时，显式生成入口必须接收合法的 UTC 秒精度 `--created-at YYYY-MM-DDThh:mm:ssZ`，不得读取系统时间；相同语义摘要必须复用已提交 `createdAt` 并拒绝覆盖，语义变化必须提供不同时间。写回稳定 `createdAt` 后，在仍省略 namespace 的 canonical 文档上计算 `documentSha256`，并将 namespace 固定为 `https://www.axialmuse.com/spdx/npm/axial-muse-website/<64-lowercase-hex-documentSha256>`；该 SPDX 标识 URI 不创建站点路由且无需可访问。最终文件摘要回填 evidence。漂移门禁在两个全新临时目录各生成一次 native SPDX，证明即使原始时间和 UUID 不同，规范化输出仍逐字节一致，并重算 namespace 与 file hash；语义变化必须改变 namespace。SPDX 2.3 要求 created 为 UTC 时间且 document namespace 为不含 `#` 的唯一绝对 URI，详见[文档创建信息规范](https://spdx.github.io/spdx-spec/v2.3/document-creation-information/)。该决定解决 #10 的稳定输出冲突，不批准真实依赖图或联网操作。
- **E-012 / 2026-07-18 — Node ESM TypeScript 测试执行**：根 `tsconfig.json` 继续只拥有 Docusaurus `bundler`/`noEmit` 生产 program；独立 `tests/tsconfig.json` 继承根配置，但必须显式覆盖 `module: "NodeNext"`、`moduleResolution: "NodeNext"`、`target: "ES2024"`、`lib: ["ES2024"]`、`types: ["node"]`、`noEmit: false`、`noEmitOnError: true`、`rootDir: ".."`、`declaration: false`、`sourceMap: false`、`incremental: false`、`composite: false` 与 `verbatimModuleSyntax: true`。配置文件中的 `include` 值精确为 `domain/**/*.test.ts` 与 `build/**/*.test.ts`，分别对应仓库根 `tests/domain/` 与 `tests/build/`；受测 `src/domain/`、`src/build/` 模块只能通过正常 import 图进入，不把浏览器或全站源码隐式纳入测试编译。根 `typecheck` 仍显式 `--noEmit` 且不包含 `tests/`。

  唯一测试入口 `scripts/quality/run-tests.mjs` 必须在系统临时目录下创建本次独占输出，以当前 `process.execPath` 运行已冻结安装的本地 TypeScript CLI，执行 `tsc -p tests/tsconfig.json --outDir <temp>`；禁止 shell、`npx`、下载 fallback、loader、原生执行 `.ts`、实验性说明符解析或写入仓库。编译成功后在临时根写入只含 `type: "module"` 与 `private: true` 的最小 `package.json`，稳定排序并显式传给当前 `process.execPath --test` 的全部编译后 `*.test.js`；没有测试、编译失败或任一测试失败均失败关闭。入口必须在 `finally` 清理临时目录，清理失败也使门禁失败；源码、内容树、`build/` 与 `dist/` 不得接收测试产物。

  所有进入该 Node 执行图的 TypeScript 相对静态 import/export 和动态 import 必须在源码中写运行时 `.js` 说明符，目录入口写 `index.js`；Node 内置模块使用 `node:`，类型专用导入使用 `import type`。无扩展名和 `.ts` 运行时说明符由 NodeNext 编译 fixture 拒绝；路径别名、`baseUrl` 裸导入和依赖偶然的目录解析由 CODE-005 模块边界检查与 fixture 失败关闭，不能假设 TypeScript 单独拒绝。主 Node 端点和最低端点执行同一编译与测试入口；fixture 至少证明合法 `.js` 说明符可编译并直接运行、无扩展名在编译期失败、空测试集失败、编译/测试/清理失败不留下仓库产物，且命令行不存在 loader 或实验解析参数。该决定解决 #11 的 `bundler` 原样输出与 Node ESM 不兼容问题，不改变浏览器源码的 Docusaurus 构建方式，也不表示 D-079 候选依赖已经完成真实准入。

  I-05 实施进一步固定源码侧 ESM 包边界：根 `package.json` 为保留 Docusaurus `3.10.2` 在 `.docusaurus/` 生成 `.js` 的 CommonJS 解释而继续不声明 `type`；`src/package.json` 与 `tests/package.json` 各自必须精确只含 `type: "module"` 与 `private: true`，使进入 NodeNext program 的普通 `.ts`/`.tsx` 生产模块和测试模块按最近包边界判为 ESM。两个局部文件不声明名称、版本、依赖、scripts、exports 或 imports，也不创建可发布 npm package；临时 emit 根仍写入同样只有两键的独立 `package.json`，不能依赖源码目录的包边界。该选择遵循 TypeScript 官方 [NodeNext module format detection](https://www.typescriptlang.org/docs/handbook/modules/reference#node16-node18-node20-nodenext)，不恢复会破坏 I-04 构建的根 module type，也不改动依赖图。

  `run-tests.mjs` CLI 为零参数、零环境旁路入口；先复用模块边界检查验证根/测试配置、局部包边界、`.js` 说明符和物理层，再只解析仓库本地冻结的 TypeScript CLI，要求当前 Node 是主或最低端点。入口在系统临时根创建 `0700` 独占目录，要求源码 `*.test.ts` 与 emit 后 `*.test.js` 一一对应，以稳定源码路径顺序显式执行；子进程输出必须把临时绝对路径映射回仓库相对源码测试路径后再传播。失败形态固定为 `TEST_ARGUMENTS`、`TEST_WORKSPACE`、`TEST_RUNTIME_NODE`、`TEST_PROGRAM`、`TEST_DEPENDENCIES`、`TEST_EMPTY`、`TEST_TEMPORARY`、`TEST_COMPILE`、`TEST_EMIT`、`TEST_EXECUTION`、`TEST_CLEANUP` 或脱敏的 `TEST_INTERNAL`，不得输出临时根或环境原值。内部导出的依赖注入只供零依赖 mutation fixture 驱动编译、执行和清理故障，不形成 CLI 参数、环境变量或生产旁路。D-079 的 `@types/node` 已随 #21 的真实依赖图完成准入；本项不修改 manifest 依赖或 lockfile。
- **E-013 / 2026-07-18 — HEAD 可达完整 Git 历史门禁**：`scripts/quality/check-content-history.mjs` 是稳定标识历史规则的唯一命令入口，公共实现位于 `scripts/quality/lib/content-history.mjs`；#24 的作者命令必须在写 staging 前调用该模块的 `checkContentHistoryCandidate({articleId,sourceName})`，不得复制算法。候选 API 只接受一篇新文章的两个身份字段，先验证真实当前工作区，再在内存叠加候选并复用同一 lineage ledger；公开 CLI 仍不接受 revision、remote、branch、浅历史容忍、自动 fetch 或候选参数。`scripts/content/frontmatter.mjs` 是当前内容扫描、Docusaurus 回调、作者工具与历史提取共用的结构化 frontmatter 解码适配；Docusaurus 回调把框架提供的 `defaultParseFrontMatter` 显式传入，独立命令则调用 `@docusaurus/utils@3.10.2` 公开导出的 `DEFAULT_PARSE_FRONT_MATTER`，历史路径只减少解码后的字段校验，禁止另写逐行、正则或分隔符近似解析。为此 `@docusaurus/utils@3.10.2` 作为直接开发依赖候选进入 D-077 的真实依赖准入，不能依赖偶然提升的传递副本；本决定不安装依赖、不生成 lockfile，也不预先批准真实 tarball 或传递图。

  Git 预检在读取历史对象前要求 realpath 后的 cwd 与 non-bare worktree top-level 完全一致，拒绝 local config 及启用 `extensions.worktreeConfig` 后的 worktree config 中的 `include.*`、任意控制字符键、`extensions.partialClone`、任意（包括空 subsection）`remote.*.promisor`/`remote.*.partialCloneFilter`，并拒绝 `rev-parse --git-path info/grafts` 指向的 legacy grafts、common object directory 下的 `pack/*.promisor` 和 `objects/info/alternates`；随后要求 `git rev-parse --is-shallow-repository` 精确返回 `false`，再解析 `HEAD^{commit}`。所有 Git 调用在子命令前固定加入 `-c core.commitGraph=false`，使父关系只读取提交对象而不信任可漂移或损坏的 single/split commit-graph。Git 子进程环境先删除全部继承的 `GIT_*`，再只设置 `GIT_NO_REPLACE_OBJECTS=1`、`GIT_OPTIONAL_LOCKS=0`、`GIT_CONFIG_SYSTEM=/dev/null`、`GIT_CONFIG_GLOBAL=/dev/null`、空 `GIT_ALLOW_PROTOCOL`、`GIT_PROTOCOL_FROM_USER=0` 与 `GIT_TERMINAL_PROMPT=0`，同时固定 `LC_ALL=C`；其中空 `GIT_ALLOW_PROTOCOL` 在当前 Git 2.43.0 已验证会把所有传输协议设为 never，不依赖该版本不支持的 `--no-lazy-fetch`。任何提交、tree 或 blob 缺失，或 Git 命令异常，均失败关闭，不联网修复、不退回当前树近似检查。

  历史闭包固定为 `git rev-list --topo-order --reverse --parents HEAD` 返回的当前 `HEAD` 全部可达祖先，只使用父边计算状态；不扫描 `--all`、未合并或已废弃分支、tag、remote-tracking ref、reflog 与本机孤立对象。每个提交只从统一结构化解码结果提取不可变身份所需的最小快照，不把未来当前完整 schema 反向套到旧提交：文章快照包含合法 `site-content/writing/<source-name>/index.md|index.mdx` 与顶层 `articleId`，注册表快照包含 E-003 声明不可复用的 project、`(project,module)`、author 与 topic ID。历史中尚未出现的内容根、注册表文件或 `writingModules` 集合按空身份集处理；文件或集合一旦存在，就必须能结构化解析且其中每个身份字段合法、无重复，其他可变或后来废弃字段不参与裁决。身份解析缺失、重复或歧义仍失败，显示名、正文和其他可变字段不进入历史 ledger。工作区当前内容作为 `HEAD` 的候选子状态另行校验，因此本地未提交改动和 CI checkout 都使用同一规则。

  每个提交分别维护当前快照以及三类历史 ledger：source-name 到唯一 articleId 的保留映射、articleId 到首次引入 commit 的 lineage、稳定注册表 ID 到首次引入 commit 的 lineage。一个 ID 首次出现时以当前 commit 为 origin；从父历史继承时，所有包含该 ID 的父 ledger 必须给出同一 origin。两个平行分支在共同祖先之后独立首次引入相同 articleId 或注册表 ID 时，即使 merge 结果只保留一个当前实体，也因 origin 不同而失败。articleId 在任一直接父当前快照仍存在且 lineage 唯一时可以连续修改或随完整目录原子改名；新 source-name 必须未映射给其他 ID，旧 source-name 继续保留给原 ID。articleId 或注册表 ID 若未出现在任何直接父当前快照、但已出现在任一父 ledger，则属于删除后重新引入并失败；同一快照中一个 ID 对应多个文章、原 source-name 改绑新 ID，或合并父历史对同一 source-name 给出不同 articleId，也都失败。该父状态规则允许正常 merge 继承任一仍存活父分支的同一 lineage，并避免用拓扑输出的总顺序误判平行分支。

  所有执行 `quality` 或该历史入口的 GitHub job 都必须使用准入后固定完整 commit SHA 的 `actions/checkout`，配置 `fetch-depth: 0`、`persist-credentials: false`，且不启用 partial/sparse checkout。`pull_request` 不覆盖 checkout `ref`，保留 `refs/pull/<n>/merge`，使合并提交的 base/head 两个父历史都进入 `HEAD` 闭包；`website-quality` 与 `node-minimum` 必须完整 checkout，不运行历史门禁的 `diagrams` 无需扩大历史。fixture 使用临时 Git 仓库覆盖连续修改、同 lineage 原子改名、source-name 改绑、articleId/注册表 ID 删除后重引、平行分支独立首次引入同一 articleId/注册表 ID、merge 第二父冲突、pre-write 候选正常路径与历史复用、工作区删除后候选碰撞、depth-1 浅克隆、缺失对象、local/worktree config include 与 promisor、空或控制字符 subsection、commit-graph 忽略、legacy grafts、partial/promisor/alternate object store 在远端 sentinel 未触发前被拒、七项 Git 环境重建和非 Git 目录；结构化解码 fixture 还要证明当前与历史入口对同一 Markdown/MDX 得到相同 frontmatter 或相同解析失败。D-097 已在工作区实现固定 SHA、完整 checkout 和关闭凭证持久化，PR 保留默认 merge ref，并依 D-100 纳入当前专题分支；尚待真实 GitHub Actions 验证。官方依据：[Docusaurus 3.10.2 默认解析器实现](https://github.com/facebook/docusaurus/blob/f37f9035584917a97a260b91fc2842cba4f8b94f/packages/docusaurus-utils/src/markdownUtils.ts)、[对应公共导出](https://github.com/facebook/docusaurus/blob/f37f9035584917a97a260b91fc2842cba4f8b94f/packages/docusaurus-utils/src/index.ts)、[Git 2.43 的协议隔离环境](https://git-scm.com/docs/git/2.43.0)、[actions/checkout 的完整历史与凭证配置](https://github.com/actions/checkout)、[GitHub `pull_request` merge ref 语义](https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows)、[`git rev-parse --is-shallow-repository`](https://git-scm.com/docs/git-rev-parse)和 [`git rev-list` 可达集合与父拓扑](https://git-scm.com/docs/git-rev-list)。E-013 原决定只固定 #12 的历史边界，没有预先批准 Action 版本或 commit SHA；这些值与 workflow 实现由 D-097 决定，并依 D-100 授权纳入本分支。
- **E-014 / 2026-07-18 — 同版本服务端 301 与静态 release**：`docs/contracts/redirects.json` 继续是旧 URL 的唯一可编辑来源，静态页面、Nginx 模板和 release metadata 都不得复制其 `reason` 或成为第二份登记表。`scripts/release/package-site.mjs` 从同一次 production `build/` 的实际公开 `index.html` 路由集合和已校验注册表确定性派生 `metadata/runtime-redirects.json` 与 `metadata/nginx/redirects.conf`，不再为旧路径生成静态跳转 HTML。运行清单 schema 固定为 `schemaVersion: "1.0.0"`、`canonicalOrigin: "https://www.axialmuse.com"` 和按 `from` ASCII 升序的 `rules`；每条规则只含 `kind`、`from`、`to`，其中 `kind` 只能是注册表派生的 `registered` 或公开页面无尾斜杠派生的 `canonical-slash`。`reason` 只留在源注册表用于审核，不进入 Nginx 注释或运行清单。

  注册表的 `from`、`to` 都必须是带末尾 `/` 的规范页面路径；每个非空段只允许 lowercase kebab-case，并拒绝 origin、查询、fragment、百分号编码、反斜杠、控制或空白字符、重复斜杠、`.`/`..` 段以及 Nginx 配置元字符。`from` 不得属于当前公开路由、保留路径或任何派生无斜杠 source；`to` 必须对应同一 `payload/` 中实际存在且预期返回 200 的规范 `index.html`，不得同时是任一重定向 source。源重复、目标缺失、静态 source 页面、自跳转、链、环或规范化冲突均失败。每个登记项生成两个 `registered` 规则：规范 `from` 和去掉其末尾 `/` 的别名都直接到最终 `to`，不得先补斜杠再跳转。`canonical-slash` 只为 `/` 之外的当前公开 HTML 路由生成，从去掉末尾 `/` 的精确路径直达规范路径；资源、`404.html`、`robots.txt` 和 `sitemap.xml` 不生成该规则。

  Nginx 文件只由固定模板生成一组 `location = <from> { return 301 https://www.axialmuse.com<to>$is_args$args; }` 精确规则；查询串显式保留，fragment 不进入 HTTP 请求。规则同时包含在根域和 `www` 的 HTTP/HTTPS 已知主机配置中，使旧路径与无斜杠路径一步到 canonical origin；通用 scheme/host 跳转只能放在各 server 的兜底 `location /`，不能使用会先于 location 搜索执行的 server 级 `return`。ACME challenge 继续由专用 location 提供，未知 Host 不加载这些规则。Nginx 的 exact location 实际匹配规范化 URI，因此 schema 在生成前排除会产生歧义或注入的源文本，不用 regex、`map`、`if` 或运行时文件读取近似路由。官方依据：[Nginx `location` 的规范化与精确匹配](https://nginx.org/en/docs/http/ngx_http_core_module.html#location)、[`return` 指令及执行顺序](https://nginx.org/en/docs/http/ngx_http_rewrite_module.html#return)和[`$is_args`/`$args` 变量](https://nginx.org/en/docs/http/ngx_http_core_module.html#variables)。

  CODE-015 的 release 清单必须记录源注册表、公开路由集合、运行清单和 Nginx 文件的 SHA-256 及规则数；内部文件清单覆盖 `payload/` 和两个可部署派生文件。服务器验证后将 payload 与这两个文件安装到同一不可变 `releases/<sha>/` 的 `payload/`、`config/`，再由 root-owned 固定脚本生成只含该 40 位 SHA 绝对 `root` 和同目录 redirect include 的 `config/site-release.conf`。基础 Nginx 模板只在配置解析时经 `current/config/site-release.conf` 选择版本；请求期 root 和 include 都是包含精确 SHA 的不可变绝对路径，不通过可变 symlink 查找文件。

  服务器另以 root-owned、非 Web Root 的 `/var/lib/axialmuse/url-exposure-ledger.json` 保存已经成功发布、或因候选 301 潜在暴露而必须预先承诺的规范 200 路径，以及所有可能对外返回过的 registered 301 边。`canonical-slash` 不单独写入历史边，但其 target 必须在 reload 前进入 `publishedRoutes`；因此首次发布新 canonical 页面时，旧 release 若没有该 target 会被正确判定为不兼容，而不是为减少授权人为忽略已可缓存的 301。后续迁移仍必须在注册表同时生成带斜杠 source 和无斜杠别名。账本是只追加的生产暴露证据，不是内容或规则编辑源，不能反向生成 `redirects.json` 或候选配置。

  账本 schema 固定为 `schemaVersion: "1.0.0"`、`canonicalOrigin: "https://www.axialmuse.com"`、按 `path` ASCII 升序的 `publishedRoutes` 和按 `from`、`to`、`kind` ASCII 升序的 `redirectEdges`；路由项只含 `path`、`firstRelease`，边项只含 `kind: "registered"`、`from`、`to`、`firstRelease`，`firstRelease` 是首次成功发布或首次可能暴露该事实的 40 位 SHA。同一 source 历史上改指新 target 时保留新旧两条边；既有项不得删除或改写，未知字段失败，全文不读取系统时间。候选兼容性以当前 payload 与全部 exact rules 定义 `resolve(path)`：规范 200 路径解析为自身，301 source 解析为其直接且实际存在的 200 target，其他路径失败。每个历史 published route 必须仍可解析；每条历史边的 `from` 与历史 `to` 必须解析到同一个当前 200 终点。由此允许二次 slug 迁移把旧 source 直接改指最新终点，但前提是曾被客户端缓存的中间 target 也继续为 200，或自身单跳到同一终点；删除历史 source、让历史 target 404、分裂到不同终点、产生链或环均失败。

  部署排他锁内先用隔离的本机 Nginx 候选实例完成配置和 HTTP 行为测试，再读取账本并验证候选。候选全部规范 200 路径、以及任何新增或改指后可能暴露的 registered 301 边，必须先并入候选账本、原子写入并 flush，随后才允许切换 `current`、执行 `nginx -t` 和 graceful reload；这同时覆盖 registered 目标与 `canonical-slash` 目标，是对 reload 已接收但公网冒烟尚未完成窗口的保守记录。激活前必须找出一个满足并入后完整账本的已验证 fallback release。存在 fallback 时，reload 或公网冒烟失败才允许整版恢复并重新验证；不存在 fallback 时，发布默认拒绝，只有单独的生产授权明确接受“激活后只向前恢复”才可继续，之后不得自动切回不兼容旧 release。公网冒烟成功且账本备份记录完成后才能标记 deployment 成功；账本损坏、丢失或写入失败一律停止发布，不能从当前注册表静默重建。301 可能被客户端长期缓存，因此兼容谓词同时保护历史 source、历史 target 和当前单跳终点，而不是只检查某个目标文件存在。[Nginx 官方 reload 行为](https://nginx.org/en/docs/beginners_guide.html#control)说明新配置失败时继续旧配置、成功时旧 worker 仅完成已有请求；[`nginx -t`](https://nginx.org/en/docs/switches.html)只证明语法和引用文件可打开，不能替代隔离候选测试或公网冒烟。该决定解决 #13 的静态跳转页返回 200、跨版本规则漂移与历史 301 回滚误判；当前仓库与服务器均未实现，封装输入的 job 字节所有权由 E-015 补齐。

  账本仅允许两种初始化：全新站点在 DNS 尚未对外且没有活动 release 时，经一次性上线授权创建空 schema；或既有站点迁移时，从可审计的生产路径/重定向清单显式导入并人工复核。本项目当前公共 DNS 无 A/AAAA，因此首次上线按前者设计；一旦初始化，账本缺失绝不能再被当作“首次部署”自动重建。
- **E-015 / 2026-07-18 — production artifact 自包含重建与字节闭包**：GitHub 的 job 使用彼此隔离的 runner，`production-artifact` 不能读取 `website-quality` 的本地 `build/`。本项目不建立中间 build artifact 交接协议；`website-quality` 继续作为 PR 和 `main` 的 required check，其本地 build 不是可部署输入。`production-artifact` 是最终可部署 build 的唯一字节所有者：仅 canonical 仓库的 `main` push，或明确选择 `refs/heads/main` 的人工 run 可以进入；job 为单一非 matrix job，直接 `needs` `website-quality`、`node-minimum`、`diagrams`、`supply-chain`，沿用默认成功传播，禁止 `always()`、`continue-on-error` 或把 failure、cancelled、skipped 当作可发布状态。GitHub 对 job、runner 与 prerequisite 的说明见 [Understanding GitHub Actions](https://docs.github.com/en/actions/get-started/understand-github-actions) 和 [Defining prerequisite jobs](https://docs.github.com/en/actions/how-tos/write-workflows/choose-what-workflows-do/use-jobs#defining-prerequisite-jobs)。

  `production-artifact` 在全新 Ubuntu runner 上以精确 `GITHUB_SHA` 完整 checkout，要求 `HEAD` 相等、`fetch-depth: 0`、`persist-credentials: false` 且无 partial/sparse checkout，并在安装前证明 checkout 中没有预存 `build/`、`dist/`；随后使用 `.nvmrc` 的精确 Node/npm、E-010 的全新 HOME/config/cache 隔离入口完成冻结安装，并以执行前后摘要证明 manifest/lock 不变。该 job 在主端点依次重新运行零依赖 `quality`、安装后独立 E-013 历史入口、类型检查、E-012 测试、Docusaurus production build、资源白名单和制品泄漏检查；最低 Node、PlantUML 与全图供应链审计不在本 job 重复，而由直接 `needs` 的独立 job 负责。上述门禁成功后不得插入第三方 Action、依赖安装、第二次 build 或其他可写 `build/` 的命令，立即运行 `package:artifact` 和独立 release 校验；下载失败后重建、读取其他 job artifact 或复用本机旧 `build/` 的 fallback 一律禁止。

  “同一 build 字节”固定为经过 CODE-015 路径校验的规范 POSIX 相对普通文件集合及每个文件的原始字节，不包含 mtime、uid/gid、mode 或空目录语义；符号链接、硬链接、特殊文件、隐藏路径、绝对/父级路径、控制字符、重复规范化路径与大小写冲突均在 build、release 和上传前失败。封装器在复制前后重算按路径排序的 SHA-256 树摘要，`release.json` 保存 `sourceBuildTreeSha256`，并证明 `payload/` 相对路径与字节一一对应；期间任何新增、删除、改名或字节变化失败。服务器安装时统一目录 `0755`、普通文件 `0644` 并按内部清单复核，因此 GitHub 外层归档的权限归一化不参与内容身份。

  最终 Action 只上传一次精确 `dist/release/`，禁止 glob、多路径、同名合并和 `overwrite: true`，使用 `if-no-files-found: error` 并显式排除隐藏文件；上传前门禁必须已经证明 release 中不存在隐藏路径。该 job 不配置 `actions/setup-node` cache、不调用 cache restore/save Action，也不读取跨 step、跨 job 或跨 run 共享/复用的依赖或 build cache；E-010 为本次 job 创建且不复用的私有 npm cache 仍是必需隔离目录。独立 release 校验通过后，仓库零依赖入口立即对 `dist/release/` 全文件树计算一次不写入 artifact 的 `releaseContentSha256`，随后不插入其他命令而执行上传；它与上传 Action 返回的外层 `artifactDigest` 分别映射为 job outputs。前者是服务器必须从下载并解包后的 exact release tree 独立重算的上传前期望值，不能从 artifact 内的自报 metadata 回填；后者绑定 GitHub 保存的外层 artifact。展示名称固定为 `axial-muse-site-<sha>-<run-id>-<run-attempt>`，真正身份是不可变 artifact ID、上述两个摘要、canonical repository、workflow run ID/attempt 与 `GITHUB_SHA`。`deploy-production` 只能直接消费这些输出，禁止按名称、pattern、latest、任意 URL 或跨 run 搜索，也不能部署中间质量输出。Action 的精确版本和 commit SHA 仍须通过 D-077 准入；upload Action 的固定源码与 `action.yml` 只需证明 artifact ID/digest 输出及不可变/overwrite 语义，摘要比较属于 deploy/服务器职责，不要求上传 Action 证明不存在的 download mismatch 行为。官方 upload action 说明 artifact ID/digest、不可变性、覆盖会换 ID、隐藏文件与权限边界：[actions/upload-artifact](https://github.com/actions/upload-artifact)。

  prerequisite jobs 与 `production-artifact` 的 `GITHUB_TOKEN` 权限固定为仅 `contents: read`，`deploy-production` 为读取 canonical ref 和当前 run artifact 元数据仅可使用 `contents: read` 与 `actions: read`；所有未列权限均为 `none`，禁止任何 write、`id-token: write` 或 attestation scope，也禁止 repository/environment Secret 进入 producer。`deploy-production` 在引用 CAM Secret 或调用腾讯云 API 前，必须用只读 GitHub API 重新验证当前 canonical `refs/heads/main` 仍精确等于本次 commit SHA，并复核 repository、run、artifact ID、`head_sha`、未过期状态与外层 digest。upload job/TAT 的 `artifactDigest` 固定为裸 64 位小写 hex，REST metadata 必须逐字节等于 `sha256:` 拼接该值；缺失或其他算法、大小写、长度和前缀形式全部失败，不做宽松归一化。不相等就结束，不上传、重建、回退或继续调用 TAT。concurrency 只提供生产互斥，不提供等待顺序或 main 新鲜度保证，不能替代此检查；发布历史 SHA 必须进入另行授权的恢复流程，普通 `workflow_dispatch` 重跑没有该权限。依据见 GitHub 的 [workflow/job permissions](https://docs.github.com/en/actions/writing-workflows/workflow-syntax-for-github-actions#permissions) 与 [concurrency 语义](https://docs.github.com/en/actions/writing-workflows/choosing-what-your-workflow-does/control-the-concurrency-of-workflows-and-jobs)。

  fixture 与 workflow 静态检查必须覆盖：任一 prerequisite 非成功、PR/fork/Dependabot/`dev`、错误 dispatch ref、matrix producer、`always()`/`continue-on-error`、`actions/setup-node` cache、cache restore/save Action、共享或复用的依赖/build cache、浮动 Action；并以正面 fixture 允许 E-010 本次 job 独占的新 npm cache；预存/空/preview build、浅历史、SHA 或 manifest/lock 漂移；质量后或封装期间文件增加、删除、改名、改字节，独立检查后到 upload 读取前整版替换 release，以及非法文件/路径；多次上传、错误目录、`overwrite`、缺失或非法 ID/digest；过宽 token 权限、旧 run 晚到、旧 run 重跑、main 在等待期间移动，以及 deploy 收到同名异 ID、错误 run/repository/head SHA、过期 artifact 或非 `production-artifact` 输出。producer 侧任何失败都不得生成最终 artifact 或调度 deploy；deploy 侧 GitHub 身份或 main 新鲜度验证失败必须在引用 CAM Secret 或调用腾讯云 API 前停止。两次 runner 对同一 SHA 的 build 字节不要求相等；本决定保证最终 build 自身完整重验，不宣称可复现构建。Artifact attestation 不解决 job 文件系统隔离，还会新增 Action、OIDC 权限与套餐边界，M0 不引入；未来外部消费者确有 provenance 需求时另行评审。该决定解决 [Issue #14](https://github.com/lyty1997/AxialMuseWebsite/issues/14)；当前 workflow、依赖、脚本和外部服务均未修改。

- **E-016 / 2026-07-23 — 单一内容装配与零公开文档适配**：#26 的 production 构建只允许一个以根级 `site-content/` 为物理根、`routeBasePath: "/"` 的 docs 实例，current-only 选项精确为 `includeCurrentVersion: true`、`onlyIncludeVersions: ["current"]`、`tags: false`。不得使用 `disableVersioning: true`：Docusaurus 3.10.2 在没有版本清单的当前内容站点会拒绝该选项，它不能代表 current-only。构建层每次完整扫描 projects/authors/topics/project-experiences 四份领域注册表、`static-public-assets.json` 和内容根后，必须把同一次 `validateProjectCatalog` 成功返回的原对象依次交给文章校验、导航、静态素材计划与框架投影；文章成功批次同样携带只在领域层可验证的 provenance。任何失败都不得返回合法子集、复用上次成功结果或退回框架默认推断。扫描器必须完整拒绝未知分区、非法 Markdown/MDX 布局、category metadata、version roots、localized 第二内容根、符号链接、特殊文件、大小写和 realpath 漂移；首轮读取结束后还要重验全部目录成员、文件身份与完整字节，Docusaurus 每次解析前后再与该批路径和字节快照核对。内容批次摘要与同次静态计划的模式、全部 allowed/excluded 条目语义、字节长度和 SHA-256 以域分隔 framing 合成唯一不公开输入摘要，共同绑定 build、独立 checker 与切换后终态 checker。这使 include、解析回调、二次 MDX 读取、跨阶段正文漂移，以及计划外未发布素材在 build/check 之间换字节都不能绕过同一输入闭包。

  当前真实内容只有两个 `planned` 项目正文而没有 production 公开 doc。已锁定的 Docusaurus 3.10.2 在该输入上先得到 `docs=[]`、`drafts=[2]`，随后官方 docs 插件的 `contentLoaded` 访问 `docs[0].id` 并崩溃；官方配置不存在既保持实例启用、又不生成占位或隐藏路由的零公开文档模式。为不改变项目状态、依赖图或公开路由，本站采用精确版本绑定的窄适配：本地 classic-derived preset 从已准入 `@docusaurus/preset-classic` 返回值中识别唯一官方 docs 插件配置，使用已准入 `@docusaurus/utils` 的模块加载能力取得该精确实现，保留其 `validateOptions`、`loadContent`、MDX loader、sidebar、route 和其余生命周期，只包装 `contentLoaded`。适配器先验证 current-only、unversioned 的 `loadedVersions/docs/drafts` 精确结构和与已验证内容的一一对应；只有 production 的公开 `docs` 精确为空时才不调用官方 `contentLoaded`，从而不生成 docs route、catch-all 或伪造 global data。有公开 doc 时，官方实现会把同版本 `drafts` 序列化为浏览器 `draftIds`；production 因此只浅克隆 `args -> content -> loadedVersions -> current version`，仅把克隆版本的 `drafts` 替换为新空数组后委托，并保留公开 `docs` 数组及成员、actions、其余 version 字段和 lifecycle `this` 的原身份。委托前后必须证明原参数对象图 descriptors 不变、受控投影不变且内容仍属当前批次，官方异常也要在该复验后原样重抛；preview 仍原样委托。版本、preset 输出、插件路径、生命周期或内容结构漂移全部失败，不能条件关闭 docs、建立第二实例、写占位 Markdown、使用 `unlisted`、等待某个项目提前公开或直接导入未声明的传递包。

  配置加载与插件初始化分责：根配置只读取并重验受控 build context，不扫描内容、不创建 content session，且可重复加载；本地 preset 在取得并改写 classic 返回的唯一官方 docs 配置前，为每次真实插件初始化完整创建一个只服务本构建的 eager session。docs 适配器、sidebar 生成器和 `createContentDataPlugin` 都由同一个已冻结 session 对象及其内嵌 adapter session 驱动，不通过惰性 Promise 重复装配，也不形成跨构建缓存。`src/build/content/index.ts` 的构建层公共 API 只导出 `loadValidatedContent`、`createParseFrontMatter`、`createSidebarItemsGenerator` 与 `createContentDataPlugin`；session 在官方 docs 构造器读取 markdown 配置前安装 `createParseFrontMatter`，并把同一导航模型交给稳定名称 `projectsSidebar`、`writingSidebar` 的 sidebar 生成器，以及只包含 `projectNavigation`、`writingNavigation` 的安全 global data。Docusaurus 的 `createData` 发生在 browser bundle 之前，单凭“不消费返回路径”不能证明私有索引未被打包；因此日期索引在 bundle 已完成的本地插件 `postBuild` 中才以原子普通文件写入本次 `generatedFilesDir/axial-muse/article-date-index.json`，独立 checker 再按同一投影逐字节核对。该 `postBuild` 只生成私有文件和输入摘要，不承担最终通过判定；索引不得进入 global data、route、static、最终 `build/` 或浏览器 bundle。production 公开文章的规范分类项目、相关项目和相关文章都必须已经公开，公开项目的 `relatedWriting` 也只能指向公开文章；draft、planned 等未发布源仍可引用已经登记且通过自身 schema 的目标。该闭包不能通过泄露未发布显示名、静默隐藏关系、移动到“通用技术”或重复身份绕过。空导航组省略；preview draft 缺 `updatedAt` 时排在有日期 draft 之后。#8 仍独占真正的 `build --dev`、全站 noindex、无 sitemap 和候选激活。

  Docusaurus 3.10.2 并行执行所有插件 `postBuild`，官方 sitemap 也在该阶段写文件，而 broken-link 检查发生在全部 `postBuild` 之后。因此 #26 的最终 route、canonical、sitemap、侧栏、私有日期索引和 E-008 素材泄漏验收不得放在本地 `postBuild`。受控 `scripts/build/build-site.mjs` 必须先取得 owner 绑定的仓库级独占构建锁，在改变候选或既有 `build/` 前回收上次固定 retired/quarantine；回收失败时现有 `build/` 保持不变。内建 `build` 子进程随后对未发布的 `.axial-muse-build-candidate-<owner>` 完整构建并写入同 owner 的私有输入摘要，再由独立 `axial-muse:check-production` external command 创建全新 session，重新扫描、核对输入摘要并串行验证候选；检查进程不继承前一进程的内存或成功状态。

  首次 checker 成功后，入口在锁内把既有 `build/` 暂存为完整 backup、把候选改名为 `build/`，再对已经位于最终路径的树运行第二次 fresh checker，关闭检查到切换之间的 TOCTOU。两次 directory rename 是受独占锁保护且可回滚的事务切换，不宣称为 POSIX 单 syscall 目录交换。终态检查成功后，旧 backup 只通过同文件系统 rename 进入唯一固定 retired 路径，输入摘要被删除，最后成功释放锁作为 commit point；commit point 后不得再执行可能改变结果的操作。retired 是完整上一版或失败候选的私有隔离，不是可发布候选，并在下一次取得锁后、任何新变更前回收，因此递归删除失败只会阻止下一次构建而不会制造“命令失败但新 build 已激活”。commit point 前任一构建、断链、检查、摘要、rename、清理或锁释放失败，都必须把调用前 `build/`（或原先不存在的状态）恢复后非零退出，并把本次候选移出可误用名称；该流程不生成 E-015/#33 的 release 身份、摘要或封装。

## 上线前必须核验

- **OD-005 服务器现场核验**：通过控制台和只读命令复核系统基线、CPU 架构、监听端口、现有软件、到期日、自动续费、快照能力和 TAT agent 状态。
- **OD-007 公安备案**：是否已有公安联网备案号；若尚未办理，网站开通后 30 日内按官方流程提交。
- **OD-009 GitHub 部署能力**：核验仓库可见性与 GitHub 方案是否支持私有仓库的 environment secrets 和 protection rules；不支持时先决定升级方案或记录使用 repository secrets 的风险接受。
- **OD-013 首版项目视觉证据**：为 DocRestore 和 VibeCoding Project Scaffold 分别准备无敏感信息的真实界面或工程截图；演示视频不阻塞本项。
- **OD-014 主站设计评审（2026-07-18 设计收口完成）**：D-051 至 D-079、E-001 至 E-016 和 active 的 [M0 主站实现 Spec](../product/m0-main-site-spec.md)已经固定上层产品方向、静态生产边界和 D-078 的内部工程委托。本轮审查的项目内容所有权、媒体 schema、发布态素材隔离、草稿预览、npm 隔离、SPDX 确定化、Node ESM 测试、完整 Git 历史门禁、同版本服务端 301、跨 job 制品字节所有权与零公开文档装配，已由 E-006 至 E-016 形成单一设计结论；[GitHub Issues #5 至 #14](https://github.com/lyty1997/AxialMuseWebsite/issues?q=is%3Aissue) 继续跟踪实现、fixture 和实际验收，不再代表活动设计缺口。上述内部设计仍按 D-078 实施；若实现需要改变产品、公开事实、数据、依赖准入、基础设施或其他保留门禁，必须另行请求用户确认。D-077 的首次真实依赖图解析与最终准入已由 #21 在 D-081/D-082 边界内完成本地验收；当前阻塞发布的仍包括目标 Action/凭证接线和 Git 操作授权，当前阻塞上线的仍包括项目公开素材、服务器、DNS、证书与备案现场核验。

## 后续非阻塞事项

- **OD-010 后续项目体验**：继续提供其他项目的名称、期望 slug、仓库地址、生产分支、构建与产物目录；不阻塞当前两个项目的主站展示。
- **OD-011 DocRestore 演示素材**：提供无敏感样例、视频成片、WebP 封面、WebVTT 中文字幕和文字摘要，并完成版权与逐帧隐私审核。
- **OD-012 DocRestore 媒体托管**：根据成片大小、主站套餐流量和预计访问量确认随站托管；不合适时先设计腾讯云对象存储方案。

## 已形成的首版基线

- **D-005 / 2026-07-12**：canonical URL 为 `https://www.axialmuse.com/`，根域由 Nginx 永久重定向到 `www`。
- **D-006 / 2026-07-12**：使用腾讯云 DNSPod、轻量应用服务器、Nginx 与 ACME，不采用 Cloudflare Pages。
- **D-007 / 2026-07-12**：生产发布由 GitHub Actions 通过最小权限 CAM 调用固定 TAT command，不开放公网部署 SSH。
- **D-008 / 2026-07-12（已被 D-028、D-029、D-051 先后替代）**：原计划 M0 维持零依赖手写静态站点。用户先确认 Git + Astro 静态生成，后于 2026-07-15 改选 Git + Docusaurus 静态构建；“无运行时后端或数据库”的当前阶段边界仍然保留。
- **D-009 / 2026-07-12**：M0 不使用访问分析、广告、第三方嵌入、评论、订阅、表单或 Cookie；Nginx access log 默认关闭，必要技术日志按期限轮转。
- **D-015 / 2026-07-13**：项目体验使用显式 DNS A 记录、精确 Nginx `server_name`、单项目证书、独立目录和独立发布权限；不使用泛解析或泛域名证书。
- **D-016 / 2026-07-13**：M0 只直接支持静态项目体验，默认 `noindex`；动态能力必须另行设计。
- **D-019 / 2026-07-13**：若未来重新批准 DocRestore 在线体验，候选拓扑为“上海轻量服务器静态前端 + 浏览器直连独立 HTTPS/WSS API”；该方案当前冻结，不属于首版实施范围。
- **D-025 / 2026-07-13**：M0 页面结构、内容状态、视觉令牌、响应式、素材、SEO、可访问性和 Definition of Done 统一受 `docs/product/m0-main-site-spec.md` 约束，评审通过后才进入页面实现。
- **D-026 / 2026-07-13**：M0 首次上线内容基线为两个可信项目和真实的技术分享空状态；完整技术文章与 DocRestore 演示视频均为上线后增量，不阻塞主站首次上线。

## 后续技术选型

- Docusaurus、Node/npm、TypeScript、内容模型、路由、主题、注册表、Linux/Ubuntu-only 执行边界、供应链协议、默认 `build/` 产物、production job 字节所有权及 GitHub Actions/TAT 交付方向已经确定。内部脚本、API、schema、测试和 CI 接线属于 D-078 委托范围；#9/#10/#21/#22/#11/#23/#5/#6/#7/#26 已完成各自实现与远端闭环，其中 #7 精确提交为 `7f2115d9f1dc5396ca0c81fc9960223644d79725`、#26 实现提交为 `91dd3c7`。D-097 至 D-099 的可信 CI 第一阶段、#12 历史门禁与 #32 workflow 契约已依 D-100 纳入当前专题分支，#24 作者集成也已完成本地验收并依 D-103 获准纳入当前专题分支提交及同名临时 ref；这些本地结果均不等于远端 CI 或 Issue 闭环。#8、#13/#14 与 #33 按各自依赖继续跟踪预览和 release 实现。后续依赖变化、新增外部工具、artifact/upload Action 与凭证、Git 发布和基础设施操作仍保留对应门禁。
- E-010 的 npm 配置与缓存隔离、官方 registry/lock 来源预检、版本闭包、旁路门禁和双端点 CLI，E-011 的确定性 SPDX 规范化与证据状态机，以及 #21 的策略/admission schema、真实候选审查、tarball/NOTICE、audit 受限报告、正式三制品、静态闭包、最终证据持有、双端点冻结安装和 composite receipt 均已通过真实图验收；#22 已消费该准入图并完成 Docusaurus/TypeScript 基线的远端闭环。D-097 至 D-099 已在工作区接通 Node 24 主/最低端点、完整历史、真实内容 production build 与静态供应链证据，并完成本地验收；普通 CI 不运行 live audit，已知 18 个 high 仍作为未修复风险跟踪。远端 CI/required checks、浏览器外部请求检查、production artifact 和受限 retention 仍由后续任务完成；任何依赖图变化都必须重新走包含 live audit 的准入闭环。
- 内容编辑采用 Markdown 默认、MDX 受控例外；M0 不启用其他解析模式，`src/components/mdx/index.ts` 白名单初始为空。任何后续 MDX 组件、浏览器行为或外部请求都必须按新增能力重新准入。
- M0 路由职责、尾斜杠、保留路径、冲突与重定向、项目内容来源、注册表、“通用技术”、跨项目关系、三栏断点和不生成的系列/主题/作者/归档路由已经由 E-001 至 E-004 固定。
- 中央身份服务的实现方式、协议、会话边界和部署位置仍待用户决定。
- 评论服务的数据字段、审核方式、删除与导出、保留周期和反滥用策略仍待用户决定。

## 内容与产品

- 第一批正式技术分享的具体主题、标题和证据材料。
- 产品服务入口何时出现；至少有一项服务完成边界设计并可真实使用后再决定是否进入主导航。
- 是否提供讨论、评论、邮件订阅或反馈表单。

## 隐私与运营

- 是否引入访问分析；如引入，需要确定供应商、数据保留周期和隐私声明。
- 是否收集邮箱或其他用户信息；如收集，需要确定用途、存储和删除方式。
- 是否需要高于 GitHub Actions 每日检查频率的外部可用性监控；如引入，需记录供应商和数据边界。
