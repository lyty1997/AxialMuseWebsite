# AxialMuseWebsite 项目规范

## 边界与真相源

- 指令优先级：系统、开发者和用户的显式指令 > 本文件 > `docs/` 设计 > `codex-rules/` 操作规则；同级冲突无法消解时请求用户确认。
- `docs/` 是定位、架构、内容、产品、部署和公开表达的真相源；`codex-rules/` 只约束 Agent 如何执行任务，不复制设计结论。
- 当前阶段与技术栈只查阅 [`docs/README.md`](docs/README.md) 及其相关设计文档，禁止在本文件维护易过期副本。
- AxialMuseWebsite 是个人项目与技术分享网站。对外内容必须可信、可追溯、可维护，不用视觉包装或营销文案掩盖事实与未实现能力。

**先定位，后实现；先内容可信，后视觉表现。**

## 用户决策门禁

1. 先从仓库、文档、配置、运行环境和工具查证事实，不让用户猜测可验证信息。
2. 查证后仍不确定，或事项只能由用户决定时，暂停所有依赖该事项的操作；说明问题、已知事实、不确定项、互斥方案、各方案影响、推荐理由和暂停范围。
3. 推荐不等于授权；不得用默认方案、保守方案、行业惯例或可否决判断替代用户确认。
4. 确认前不得改动依赖该决定的文档、代码、配置、公开内容或基础设施，也不得提交、推送、创建或合并 PR，或操作服务器、DNS、云资源。
5. 确认后先复述选择、授权范围、影响和验证方式，将决定写入设计文档或决策记录，再实施；普通实现细节不得改变已确认意图和边界。
6. 用户可以显式委托一个有明确边界的事项集合由 Agent 做工程判断；委托范围、排除项和验证方式必须先写入决策记录。委托范围内不再逐项请求确认，范围外仍执行上述门禁。

技术栈、系统或信息架构、视觉方向、公开文案与内容范围、数据与隐私、外部服务、费用、域名与部署、运维责任、破坏性操作和 Git 发布流程均属于典型用户决策事项。

## 任务入口

1. 阅读 `docs/README.md` 和任务相关设计文档。
2. 变更仓库内容时读取 [`codex-workflow.md`](codex-rules/rules/codex-workflow.md)，并通过 [`global-AGENTS.md`](codex-rules/global-AGENTS.md) 只加载命中任务类型的主题规则；禁止批量加载整个 `codex-rules/`。
3. 仅在任务涉及对应脚本或环境时读取 [`known-issues.md`](codex-rules/known-issues.md)。

## 通用约束

- 手工编辑使用 `apply_patch`，保留用户已有改动；未经明确要求和风险确认，不执行破坏性操作。
- 不提交、打印或写入 API Key、Secret、token、密码、私钥、真实隐私数据、未公开商业计划或客户数据。
- 与用户对话使用中文；文档、注释和 UI 的细则按语言规则执行。
- 修改后运行与风险相称的检查；结束时汇报改动、验证结果、未完成项，以及是否引入第三方服务或用户数据处理。
