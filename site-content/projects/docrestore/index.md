## 问题背景

把多张文档照片转化为结构清晰、可编辑并可追溯的 Markdown，而不是只得到彼此孤立的 OCR 文本。单张识别结果只有局部文字，仍需要处理页面顺序、重复内容、版面噪声和跨页结构，才能形成可继续编辑的文档。

## 能力与架构

当前代码以分阶段流水线组织 OCR、清洗、合并、脱敏、精修和输出。处理链覆盖图片与 PDF 输入、OCR 识别、内容清洗与去重、可选 PII 脱敏、可选 LLM 精修以及 Markdown 结果输出；任务和结果可以持久化，处理进度可通过 WebSocket 传递。

这条链路把每个处理阶段保留为可定位的工程步骤，便于核对中间结果和失败位置，而不是把所有转换隐藏在一次不可解释的调用里。

## 关键取舍

- 采用分阶段处理流水线组织 OCR、清洗、合并、脱敏、精修和输出，让各阶段的输入、输出和失败边界可以分别检查。
- 主站首次上线只展示源码，不提供在线体验；演示视频作为后续增量，只有素材完整并通过公开性审核后才加入。
- 脱敏和 LLM 精修保持为可选阶段，不把它们写成对所有输入都成立的质量或隐私保证。

## 当前限制

- 当前没有公共在线体验；后端只用于本地开发、私有运行和录制演示，不是公网多用户服务。
- 演示视频、封面、字幕和公开视觉证据尚未完成，不能把待准备素材表述为已经交付。
- OCR、脱敏与 LLM 精修的实际效果需要针对具体输入核验，源文档和处理结果仍需人工复核。

## 证据说明

- 项目详情页的“查看源码”入口由项目注册表派生；目标公开仓库中的 README、处理流水线代码和测试用于核对当前实现。
- [项目展示与未来体验设计](../../../docs/projects/docrestore-experience.md)记录主站公开边界，以及尚未实施的在线体验、安全和数据门禁。
- 当前证据只支持源码和已核对的工程事实；视觉证据准备完成前，不以占位图或未审核录屏替代。

## 复盘

项目说明需要同时保留处理链的可追溯性和当前发布边界。先把已实现步骤、可选能力与未完成事项分开陈述，再逐步补充经过审核的视觉证据，可以避免用尚未上线的体验掩盖真实工程状态。
