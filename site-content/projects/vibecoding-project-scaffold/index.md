## 问题背景

新项目通常缺少统一的设计真相源、Agent 执行规则和自动质量门禁，后续容易产生文档、代码与流程漂移。使用 AI 编码工具时，如果只关注生成代码，这些缺口会更早暴露，也更难在项目扩大后补齐。

## 能力与架构

- `scripts/init.mjs` 可以替换项目名、品牌、slug、定位和许可证归属等模板占位内容。
- `docs/`、`AGENTS.md`、`CLAUDE.md` 与 `codex-rules/` 共同建立设计、事实和执行规则的分层边界。
- `npm run quality` 使用 Node.js 内置能力检查 JavaScript 语法、Markdown 链接与索引、契约词表、常见密钥形态和静态站点入口，首版没有第三方 npm 运行依赖。
- GitHub Actions 与 pre-commit 都运行 `npm run quality`；commit-msg Git hook 另行校验双语提交主题，CI 还单独编译 PlantUML 图。
- 仓库还提供 PlantUML 检查与渲染、跨机器同步和远程预览脚本；具体环境仍需要使用者配置并验证。

## 关键取舍

- 脚手架不绑定前端或后端框架，先提供可解释的工程治理基线，再由具体项目完成技术选型。
- 初始化脚本替换模板占位符，但不替具体项目决定产品、数据、部署或内容边界。
- 以设计文档、Agent 规则、零第三方依赖质量脚本、CI 和 Git hooks 形成可执行基线。
- 项目采用 Apache License 2.0 开源；现有证据只确认许可证事实，不补写未经确认的选择动机。

## 当前限制

- 这套脚手架不替代具体应用框架，也不承诺自动生成完整产品。
- 使用者仍需根据真实项目决定前端、后端、数据库、部署与内容边界，并把决定写入对应真相源。
- 跨机器同步、预览和技术栈配方只是可复用入口，不能替代目标环境中的实际配置与验收。
- 当前只提供源码，不运行公共在线体验，也不收集访问者输入。

## 证据说明

- 项目详情页的“查看源码”入口由项目注册表派生；目标公开仓库中的初始化脚本、质量脚本、CI 配置、Git hooks 和项目文档用于核对上述能力。
- [项目展示设计](https://github.com/lyty1997/AxialMuseWebsite/blob/main/docs/projects/vibecoding-project-scaffold.md)继续记录仓库事实、页面动作、视觉证据要求、公开边界和验收门禁。
- 公开说明只覆盖仓库中实际存在并可以复现的行为，不把模板能力扩写为对所有项目都成立的保证。

## 复盘

可复用基线的价值不在于替所有项目预先选好答案，而在于让项目从开始就有明确真相源、可执行规则和失败可见的检查。模板仍需要随具体项目裁剪；只有在目标仓库和目标环境中运行过的门禁，才能作为该项目的验收证据。
